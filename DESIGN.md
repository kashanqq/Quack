# Дизайн Quack!

Документ описывает, как выглядит и ведёт себя интерфейс: палитру, шрифты, экраны, компоненты, анимации и адаптивность. Что продукт делает и по каким правилам — в [product-logic.md](product-logic.md), как он устроен технически — в [arch-logic.md](arch-logic.md). Реализация — в [frontend/](frontend/).

Все значения ниже взяты из текущего кода фронтенда. Если меняете токен в коде — обновите его здесь.

---

## 1. Принципы

- **Тёплая тёмная тема.** Коричнево-серый фон и один оранжевый акцент. Акцент — для действий и того, что требует внимания: отправка, подтверждение, выбранное, отличия в сравнении. Единственное исключение — светлые панели секции «Quack?» на лендинге: там витрина продукта нарочно отделена от самого продукта.
- **Чат — главная поверхность.** Подбор идёт в разговоре. Программы приходят в чат карточками, а боковые панели дублируют и расширяют то, что уже сказано.
- **Честные данные.** Реалистичность показывается только словами из трёх уровней, без процентов и обещаний. У каждого уровня видно, из каких факторов он сложился, допущения подписаны. Вымышленные данные помечены «демо».
- **Никакой лишней дисциплины.** Никаких галочек «прошёл» и укоров. Интерфейс подсказывает и предлагает, решает ученик.
- **Живость без шума.** Анимации объясняют переходы (что откуда появилось, что изменилось). Утка — единственный «просто весёлый» элемент, и она редкая.

---

## 2. Цвета

### Основа

| Токен | Значение | Где |
|---|---|---|
| `--bg` | `#2a2725` | Фон всех страниц, фон загрузки |
| `--header-bg` | `#433f3c` | Хэдер раздела «Выбор» |
| — | `#4c4743` | Плашка хэдера лендинга |
| `--sidebar-bg` | `#242120` | Левая колонка, панель «Подробнее» |
| `--bubble-bg` | `#1f1d1b` | Пузыри чата, карточки программ, факторы |
| `--panel-head-bg` | `#36322f` | Шапка «Как я тебя вижу», меню пользователя |
| `--panel-body-bg` | `#1f1d1b` | Тело «Как я тебя вижу» |
| `--dropdown-bg` | `#242120` | Выпадающие меню лендинга |

### Текст

| Токен | Значение | Где |
|---|---|---|
| `--text` | `#ffffff` | Основной текст |
| `--text-dim` | `rgba(255,255,255,.6)` | Приветствие, второстепенные заголовки, неактивные иконки |
| `--text-faint` | `rgba(255,255,255,.5)` | Плейсхолдеры, подписи, пустые состояния |
| `--text-muted` | `#877d73` | Подзаголовок лендинга |

### Акцент

| Токен | Значение | Где |
|---|---|---|
| `--accent` | `#ff7a00` | Кнопки действий, «!» в логотипе, заполнение прогресса, отправка |
| `--accent-soft` | `#ff8f28` | Выделение в подзаголовке лендинга |
| `--accent-label` | `#ffac4a` | Названия полей профиля, «Подходит тебе» |
| `--accent-tint` | `rgba(255,145,0,.2)` | Фон выбранной вкладки или режима, нажатые кнопки |
| `--edit-bg` | `#5a4838` | Кнопка «Исправить» под резюме |

### Статусы

| Смысл | Цвет |
|---|---|
| Реалистично / фактор в порядке / этап пройден | `#59b054` |
| Стоит попробовать / этап в работе | `#cece51` |
| Маловероятно / этап впереди | `#d9d9d9` |
| Фактор ниже требования | `--accent` |
| Фактор неизвестен | `#8a8580` |

### Прочее

- Элементы управления (трек прогресса, неактивная кнопка отправки): `#d9d9d9`.
- Ползунок скроллбара `#777777`, в панели профиля `#57524e`, тонкие скроллбары `#4a4643`.
- Подложка подписей нодов роадмапа: `#131415`.
- Утка: тело `#ffd84d`, крыло `#f2b23a`, клюв `#ff7a00`, контур `--duck-outline` `#2b2826`.
- Секция «Quack?» на лендинге — единственная светлая поверхность в продукте: панели `#d8d3cd`, пузырь ассистента `#ece8e3`, текст на них `#2a2725`, приглушённый текст `rgba(42,39,37,.62)`, активная строка списка и пузырь ученика `#4c4743`, оранжевый на светлом `#b4560c` (обычный `--accent` на таком фоне не читается).
- Фоновая сетка лендинга: линии белые `rgba(255,255,255,.055)`, у вершины купола под курсором перетекают в `#ff7a00` и доходят до альфы `0.32`.

---

## 3. Шрифты

| Шрифт | Роль |
|---|---|
| **Intel One Mono** | Логотип, навигация и заголовок лендинга, заголовки секции «Quack?», названия универов и пунктов списка, имя пользователя, текст загрузки |
| **Raleway** | Кнопка «Стартуем!»; весь текст раздела «Выбор»: чат, профиль, панели, карточки; подписи и карточки в секции «Quack?» |
| **Inter** | Подписи нодов роадмапа |

Логотип — `Quack!`, где `!` всегда `--accent`.

Лендинг на русском. Все строки собраны в `frontend/src/components/home/copy.ts` — разметка их не содержит, чтобы вторая локаль сводилась к ещё одному объекту той же формы. Шрифт кнопки «Стартуем!» — Raleway, а не Khula из макета: в Khula нет кириллицы.

Ключевые размеры:
- **Лендинг:**
  - заголовок 90px;
  - подзаголовок 22px;
  - навигация 18px;
  - «Стартуем!» 31px;
  - «Quack?» 64px, пункты списка 16px, название универа 25px, реплики чата 16px.
- **«Выбор»:**
  - логотип 26px;
  - приветствие 64px;
  - сообщения 24/28px;
  - поле ввода 24px;
  - заголовок профиля 22–36px и строки профиля 16–22px — меняются с шириной панели;
  - текст в левой колонке 13–15px.

---

## 4. Форма

| Элемент | Скругление |
|---|---|
| Пузырь сообщения, шапка профиля | 28px |
| Нижний левый угол тела профиля | 45px |
| Карточка программы | 24px |
| Поле ввода | 22px |
| Кнопка «Стартуем!» | 44px |
| Панели секции «Quack?» | 26px, активная строка списка 18px |
| Плашка хэдера лендинга | 34px |
| Переключатели, кнопки-иконки | 10–14px |
| Бейджи, пилюли | полностью круглые |

Тени — только у всплывающих слоёв: хэдер, меню, панель «Подробнее», профиль поверх чата, тост.

---

## 5. Экраны

Макеты: [Figma](https://www.figma.com/design/E3hKJLDvq8pZmlG5SdwjJ9/Untitled) — страница 1 (лендинг) и страница 2 (раздел «Выбор»).

### 5.1 Лендинг `/`

Лендинг — два экрана ровно по высоте окна: первый с роадмапом, второй с секцией «Quack?». Под ними на всю страницу закреплена фоновая сетка — она не скроллится, двигается только содержимое.

**Фоновая сетка.** Canvas во всю область окна, `position: fixed`, за содержимым. Под курсором сетка выгибается к зрителю: точки расходятся от курсора по гауссову затуханию (радиус 200px, максимальное увеличение 0.8), линии там же становятся толще, ярче и перетекают в оранжевый, на пересечениях загораются точки. Купол догоняет курсор с инерцией, при уходе курсора гаснет, а цикл перерисовки останавливается. Без зависимостей, обычный 2D-контекст.

**Экран 1 — хэдер и роадмап:**

1. **Хэдер-плашка** шириной 1028px:
   - слева логотип;
   - в центре пункты меню «Возможности», «Ресурсы», «Подписки» с выпадающими меню при наведении;
   - справа «Войти» и «Регистрация».
   - При наведении на «Регистрацию» оранжевый круг перетекает под надпись, а стрелка исчезает. Надпись — «Регистрация», а не «Зарегистрироваться»: длинная форма не помещается в плашку.
2. **Заголовок** `Quack!`.
3. **Роадмап пути ученика** из пяти нодов: Анкета → Программы → План → Наборы → Экзамен. Ноды стоят зигзагом, их соединяют линии.
4. **Кнопка «Стартуем!»** ведёт в раздел «Выбор».
5. **Подзаголовок** под кнопкой, с выделенным «правильного университета».
6. **Подсказка «листай вниз»** с покачивающейся стрелкой у нижнего края.

**Экран 2 — секция «Quack?»:** заголовок и две светлые панели. Слева список из трёх пунктов, за активным ездит скруглённый прямоугольник; справа — панель с содержимым активного пункта. При появлении секции на экране заголовок, список и панель выезжают по очереди. По умолчанию открыт первый пункт; пока курсор не на списке, пункты сменяются сами каждые 8 с, при наведении автосмена останавливается и слушается только курсор.

1. **Выбор университета** — карточки программ сменяются каждые 5 с: название, направление, город и страна, вступительные баллы, стоимость, дедлайн и теги «кому подойдёт». Данные — тот же демо-набор, что и в «Выборе», с пометкой «демо». Снизу полоски-переключатели, активная заполняется оранжевым за те же 5 с.
2. **Подготовка с ассистентом** — примеры диалогов, новый каждые 10 с. Реплики появляются по одной, перед ответом ассистента — три прыгающие точки. Аватар ассистента — пиксельная утка.
3. **Свой темп** — три пиксельные утки одного размера: «Спринт», «Ровный шаг», «На чиле». Отличается только скорость покачивания и взмаха, так что ни один темп не выглядит правильным.

### 5.2 Раздел «Выбор» `/choice`

Три колонки: левая колонка, чат, «Как я тебя вижу».

**Хэдер (60px):** логотип (ведёт на лендинг), аватар и имя с меню («Начать заново», «Выйти»). Справа — кнопка показа и скрытия «Как я тебя вижу».

**Левая колонка** — навигация в стиле Claude:
1. **Переключатель разделов** «Выбор / Подготовка» — две иконки с подписями. Подготовка пока заглушка: пункты Обзор, Сеты и Текущий сет с пометкой «скоро».
2. **«Новый чат».**
3. **Программы** с вкладками:
   - Подборка — список с цветной точкой уровня и ☆;
   - Избранное;
   - Сравнение — список и кнопка «Открыть сравнение».
4. **Чаты** — история разговоров: активный чат подсвечен, при наведении появляется удаление.

В свёрнутом виде колонка превращается в полосу иконок 72px:
- развернуть колонку;
- два режима;
- новый чат;
- подборка, избранное и сравнение со счётчиками;
- история.

**Чат:**
- **Пустой чат:** по центру крупное приветствие, фразы сменяются каждые 3.6 с:
  - «Привет! Куда целишься после школы?»
  - «IT, Бизнес, Медицина, Еще не определился?»
  
  Поле ввода широкое: до 1095px.
- **Разговор:**
  - сообщения ученика справа, ассистента слева;
  - поле ввода сужается до 727px, его подсказка меняется под следующий вопрос ассистента.
- **Резюме понимания:** под ним кнопки ✓ «Всё верно» (оранжевая) и ✏ «Исправить». Правка идёт прямо в пузыре, исправленный текст уходит сообщением ученика, и ассистент присылает обновлённое резюме.
- **Подборка:** после подтверждения в чат приходит горизонтальная лента карточек программ. На карточке:
  - уровень реалистичности и метка «демо»;
  - вуз, программа, город и цена;
  - «Подходит тебе» / «Но»;
  - кнопки ☆, «Сравнить», «Подробнее».
- **«Подробнее»** — панель, выезжающая слева поверх чата:
  - факторы реалистичности с цветными точками;
  - «Подходит тебе»;
  - среда;
  - кнопки «В избранное» и «Сравнить».
- **Сравнение** открывается на месте чата. Это таблица из 2–4 колонок, строки с различиями отмечены оранжевой чертой. Внизу итог: «Одинаково по: … Для тебя разница — …».

**«Как я тебя вижу»:**
- шапка с заголовком и кнопкой скрытия;
- поля профиля «Название — значение»; новое поле въезжает, изменённое подсвечивается;
- внизу «Готовность профиля» с оранжевой полосой: до 75% за заполненный профиль, 100% после подтверждения резюме.

### 5.3 Раздел «Подготовка»

Пока заглушка. Логика и дизайн будут отдельно (см. `product-logic.md` §4: Обзор, Первичный замер, Сеты, Текущий сет, Чат подготовки).

---

## 6. Компоненты

| Компонент | Поведение |
|---|---|
| Кнопка-иконка | 36×36, иконка `--text-dim`, при наведении светлый фон и белая иконка; нажатая (`aria-pressed`) окрашена в акцент |
| Переключатель, вкладки | Тёмная подложка, выбранный пункт — `--accent-tint` с тонкой оранжевой обводкой |
| Бейдж уровня | Пилюля с цветной точкой и словом уровня |
| Метка «демо» | Пилюля с пунктирной рамкой |
| Счётчик | Круглый бейдж; на полосе иконок оранжевый, «выпрыгивает» при изменении |
| Скроллбар чата и профиля | Свой: стрелки ▲▼ из Figma, ползунок можно тянуть |
| Край панели | Полоса 12px с курсором `col-resize`, при наведении оранжевая линия; двигается стрелками с клавиатуры |
| Тост | Внизу по центру чата, исчезает через 2.6 с |

**Иконки** — набор [Lucide](https://lucide.dev/) (лицензия ISC), SVG в `frontend/public/assets/icon-*.svg`. В интерфейсе красятся через `mask` в цвет текста. Графика из макетов (стрелки, ноды, точки, аватар, отправка) экспортирована из Figma.

**Утка** — своя плоская SVG из трёх цветов: тело, крыло, клюв.

---

## 7. Анимации

Кривые: `--ease` = `cubic-bezier(0.65, 0, 0.35, 1)` для перемещений панелей, `--ease-out` = `cubic-bezier(0.2, 0.8, 0.2, 1)` для появлений.

| Что | Как |
|---|---|
| «Регистрация» (лендинг) | Круг растягивается под надпись, стрелка уменьшается и гаснет, 0.45 с |
| Фоновая сетка (лендинг) | Купол под курсором: линии выгибаются, ярчеют и теплеют. Курсор догоняется с коэффициентом 0.12 за кадр, при уходе купол опадает и цикл перерисовки останавливается |
| Первый экран (лендинг) | Заголовок, кнопка и подзаголовок выплывают снизу с шагом 0.15 с, подсказка «листай вниз» — через 0.8 с и дальше покачивается |
| Появление секции «Quack?» | Заголовок, список и панель выезжают при попадании в кадр: сверху, слева и справа, с шагом 0.1 с. Фон при этом неподвижен |
| Список «Quack?» | Прямоугольник за активным пунктом переезжает за 0.5 с, текст активного пункта белеет за 0.35 с |
| Панели «Quack?» | Уходящая панель гаснет за 0.2 с, следующая появляется за 0.4 с с задержкой 0.18 с — так два текста не накладываются |
| Карточки универов | Кросс-фейд 0.6 с со сдвигом снизу, полоска активного переключателя заполняется за 5 с |
| Демо-чат | Реплики появляются по одной снизу вверх (0.42 с), перед ответом ассистента три прыгающие точки ~0.9 с |
| Пиксельные утки | Покадровая анимация `steps()`: тело покачивается на 1 пиксель, крыло отстаёт на такт, лапы переступают. Такт: 0.26 с «Спринт», 0.5 с «Ровный шаг», 0.95 с «На чиле» |
| Роадмап (лендинг) | Запускается, когда роадмап виден на экране. Каждые 1.8 с появляется новый нод, группа сдвигается и остаётся по центру, линия тянется к новому ноду, точки идут серый → жёлтый → зелёный. Всего около 9.3 с, в конце все зелёные |
| Переход между страницами | Загрузка с уткой, которая покачивается и машет крыльями, и «Quack!…». Показывается 0.9 с до перехода и 0.5 с после |
| Летающая утка | Первый раз через 15–40 с, дальше раз в 45–75 с. Случайные высота, направление, размер и скорость (6–10 с на пролёт). На фоновой вкладке не летает |
| Приветствие | Смена фраз с размытием и сдвигом, 0.8 с |
| Начало разговора | Приветствие уходит вверх, поле ввода сужается, левая колонка сворачивается в иконки, профиль выезжает справа; колонки перестраиваются за 0.6 с |
| Сообщения | Появление снизу с лёгким увеличением. Перед ответом ассистента три прыгающие точки, затем печать текста (около 1 с) |
| Кнопки ✓ / ✏ | Появляются «pop», уходят с поворотом |
| Профиль | Новые строки въезжают, изменённое значение вспыхивает оранжевым, прогресс растёт за 0.9 с |
| Карточки программ | Появляются по очереди с шагом 90 мс, при наведении приподнимаются |
| Панели | «Подробнее» выезжает за 0.45 с с затемнением фона, сравнение появляется снизу, строки таблицы — по очереди |

Если в системе включено «уменьшение движения», анимации и полёты утки отключаются, роадмап сразу показан в финальном состоянии, секция «Quack?» показана целиком без выезда, купол под курсором не строится, а карточки, диалоги и утки стоят на первом варианте.

---

## 8. Раскладка и адаптивность

### Панели «Выбора»

| Панель | По умолчанию | Диапазон | Поведение |
|---|---|---|---|
| Левая колонка | 302px | 240–440px | Если отпустить уже 160px, сворачивается в полосу иконок 72px. В начале разговора сворачивается сама, если её не двигали вручную |
| «Как я тебя вижу» | 620px | 380–760px | Если отпустить уже 300px, скрывается. Скрыть и вернуть можно кнопками |
| Чат | остаток | не меньше 460px | Если профилю не хватает места рядом с чатом, он открывается поверх чата |

Ширины панелей запоминаются в браузере.

### Брейкпоинты

| Ширина окна | Вид |
|---|---|
| > 1100px | Полный вид |
| 761–1100px | Та же раскладка, текст чата мельче. Сюда попадают ноутбуки с масштабом Windows 125–175% |
| ≤ 760px | Телефон: одна колонка, левая колонка — шторка из кнопки в хэдере, профиль под чатом, без перетаскивания |

Лендинг: до 1080px пункты меню сближаются, до 960px меню скрывается, заголовок и роадмап уменьшаются.

---

## 9. Доступность

- Кнопки-иконки подписаны (`aria-label`), вкладки и переключатели размечены `role="tab"` и `aria-selected`.
- Края панелей — `role="separator"` с текущим значением, двигаются стрелками.
- Сообщения чата объявляются экранным дикторам (`aria-live`).
- Скрытые панели помечены `aria-hidden`.
- Учитывается «уменьшение движения» (см. §7).
