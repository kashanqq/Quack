# Ручной чек-лист фазы 1 — B2

Семь пунктов из конца §6 ТЗ `docs/tz/30-B2.md`, дословно. Живой провайдер,
отметки руками. Полные результаты пробы (п.1–3) и рассуждение о выборе
провайдера — в `docs/decisions/llm-provider.md`.

| № | Пункт | Как проверяю | Результат | Дата |
| :-- | :---- | :---- | :---- | :---- |
| 1 | `structured(ObservationOut)` на трёх русских фрагментах — валидный JSON с первого раза ≥ 2 из 3; `skill_id` из переданного списка | `PYTHONPATH=. uv run python scripts/llm_probe.py`, раздел вывода «§2 item 1»; в ответе — «Итого валидны с первого раза: N из 3» и список `skill_id` по фрагменту | **Пройден.** 2 из 3 валидны с первого раза (третий спасён встроенным повтором `structured()`); все `skill_id` во всех трёх фрагментах — из переданного списка. Подробности и таблица — `docs/decisions/llm-provider.md`, «Результаты пробы» п.1 | 18.09.2026 |
| 2 | `stream()` с инструментом `get_time` — инструмент вызван, текст после него, первый `text_delta` < 3 с | тот же прогон, раздел вывода «§2 item 2»; в ответе — «инструмент вызван: True», «текст после вызова: True», «время до первого text_delta: N» | **Частично.** Инструмент вызван, текст после вызова есть (через продолжение потока синтетическим результатом инструмента) — но время до первого `text_delta` 12.98 с, критерий < 3 с не пройден. Не блокирует Ф1 (эхо-режим `router.run_chat` инструменты не передаёт) — открытый риск на Ф2, см. `docs/decisions/llm-provider.md` | 18.09.2026 |
| 3 | То же при `LLM_STRUCTURED_MODE=tool` для `structured()` | `LLM_STRUCTURED_MODE=tool PYTHONPATH=. uv run python scripts/llm_probe.py`, тот же вывод | **Пройден по structured (п.1), тот же результат по stream, что в п.2.** 2 из 3 валидны с первого раза в режиме `tool`, все `skill_id` из списка; `stream()` в этом прогоне — инструмент вызван, текст после вызова есть, время до первого `text_delta` 14.59 с (тот же провал критерия < 3 с, не зависит от режима structured output). Подробности — `docs/decisions/llm-provider.md` | 18.09.2026 |
| 4 | 20 вызовов `bulk` подряд — ни одного 429 от провайдера (rate-limit работает) | `uv run python -c "..."` — 20 последовательных `client.complete(..., slot=\"bulk\")` и подсчёт `RateLimitError` | **Отложено.** Не выполнялось. Причина: вне «ровно тех вызовов, что описаны в §2» этого шага — 20 платных вызовов `bulk` с `effort=high` (порядка $0.1–0.2 по прайсу V4-Pro) ради проверки rate-limit, значения которого (`LLM_RPM_*=10`) сами ещё не подтверждены дашбордом (см. п.3 в `docs/decisions/llm-provider.md`) — тестировать лимит, который не проверен, преждевременно. Сделать после того, как лимиты аккаунта проверены и `LLM_RPM_*` пересчитаны | — |
| 5 | Отключить сеть → `GET /health` показывает `llm_status: down` в течение минуты; включить → `ok` в течение трёх минут | `GET /health` руками (curl/браузер) при отключённой и включённой сети | **Заблокировано B3.** `app/api/`, `app/main.py` в Ф1 — пустые однострочные заглушки (вне зоны B2, `docs/tz/30-B2.md` §8: SSE-транспорт и роутеры — B3), `/health` не существует как рабочий эндпоинт. Запись — `docs/sync-log.md` | — |
| 6 | `LLM_FORCE_DOWN=1` → `/health` `down`, `POST /chat/selection/messages` → 503 `llm_unavailable` | `LLM_FORCE_DOWN=1`, `GET /health` и `POST /chat/selection/messages` руками | **Заблокировано B3.** Те же отсутствующие эндпоинты, что в п.5 | — |
| 7 | Эхо-чат через `curl -N` с cookie: токены приходят потоком, последнее событие `done` | `curl -N` с cookie `quack_token` на `POST /chat/selection/messages` | **Заблокировано B3.** Тот же отсутствующий SSE-транспорт `/chat`, что в п.5–6; `agents.router.run_chat` (B2) сам по себе готов и проверен фейком (`docs/tz/notes/b2-progress.md`, строка «Router — run_chat, эхо») — не хватает только проводки B3 | — |

## Итог

Пункты 1–3 пройдены (2 из 3 — не 3 из 3 — по структурированному выводу;
критерий времени до первого токена в потоке с инструментом не пройден,
задокументировано и не блокирует Ф1). Пункт 4 сознательно отложен.
Пункты 5–7 заблокированы отсутствием слоя B3 (`app/api/`, `app/main.py`)
— технически невыполнимы до его появления, не являются недоработкой B2.
