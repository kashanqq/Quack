# B2 — рабочий журнал, фаза 1

| Шаг ТЗ | Статус | Дата | Чем проверено |
| :---- | :---- | :---- | :---- |
| Клиент — интерфейс и транспорт (§3.1–3.2) | сделан с оговоркой | 18.09.2026 | `app/llm/client.py`: `LLMLike`, `LLMClient.__init__/complete/stream` реализованы (только транспорт); `structured()`/`status()` — `NotImplementedError`; `_acquire`/`_check_breaker`/`_record` — пустые хуки, расставлены по местам. Проверено: `ruff check` чисто, импорт и `isinstance(c, LLMLike)` в `.venv` (Python 3.12.10), смоук `_model_for`/`_timeout_for`/`_to_openai_messages`. Живой провайдер не дёргался — rate-limit/breaker ещё не реализованы. |
| Клиент — rate-limit и circuit breaker (§3.3–3.4) | сделан | 18.09.2026 | `_acquire`: INCR/EXPIRE 60 на `keys.llm_ratelimit(slot)`, при превышении — DECR (реservация) и ожидание циклом по `time.monotonic()` (5с chat / 30с bulk), иначе `LLMUnavailable("rate limit")`. `_check_breaker`/`_record`: классификация по типам `openai` (`APITimeoutError`, `APIConnectionError`, `RateLimitError`, `APIStatusError.status_code>=500`), счётчик `keys.llm_errors()` (INCR+EXPIRE 60), ≥3 → `keys.llm_status()="down"` TTL 120 + маркер `keys.llm_probe()`; после истечения TTL — пробный вызов, снимает маркер при успехе или переоткрывает `down` при ошибке. `LLM_FORCE_DOWN` — без Redis и сети. `status()` — `ok` при недоступном Redis + warning. Проверено смоук-скриптом на подменённом транспорте (`fakeredis`): 3 таймаута → `down` (TTL≤120); блокировка без обращения к транспорту; удаление ключа статуса → `degraded` → `ok` после успешной пробы; `LLM_FORCE_DOWN=1` → `down` без транспорта; три 400 → статус остаётся `ok`; `LLM_RPM_BULK=2` → третий вызов ловит `LLMUnavailable("rate limit")`. Ретрай для `bulk` (1с при таймауте/5xx/429) реализован в `_create_completion`; для `stream()` — только до первого отданного события. |
| Клиент — structured output (§3.5) | сделан | 18.09.2026 | `structured()`: `response_format`/`tool` по `settings.LLM_STRUCTURED_MODE`, `strict` только при `LLM_STRICT_SCHEMA`; разбор — обрезка до первой `{`/последней `}` и `model_validate_json`; невалидно → один повтор с добавленным `user`-сообщением о провале валидации, снова невалидно → `LLMUnavailable("structured output failed")` + лог, обрезанный до 200 символов. Невалидный JSON не трогает счётчик брейкера. Проверено смоук-скриптом: ограждённый JSON с неверным типом поля на первом ответе → чинится вторым валидным (второй запрос к транспорту содержит сообщение о провале валидации); два невалидных подряд → `LLMUnavailable`. |
| `reasoning_effort` по слоту (provider.md) | сделан | 18.09.2026 | `_reasoning_for(slot)` читает `LLM_REASONING_CHAT`/`LLM_REASONING_BULK`, подставляется в `chat.completions.create` как именованный аргумент (сигнатура SDK `openai==3.14.1` принимает `reasoning_effort` напрямую — проверено в `.venv`, `extra_body` не понадобился); `None` → аргумент не передаётся. Блок рассуждений в `stream()` — явная ветка-заглушка на `delta.reasoning_content`/`delta.reasoning`, отбрасывается, не попадает в `TextDelta`. |
| Fake-клиент (§3.6) | сделан | 18.09.2026 | `app/llm/fake.py`: `FakeLLMClient(script)`, `FakeTurn = LLMResult \| list[StreamEvent] \| Exception \| BaseModel`, `FakeCall(method, messages, slot, tools)`. `structured()` реализует тот же двухпопыточный цикл, что и `LLMClient.structured` (через общий помощник, см. ниже), включая добавление сообщения о провале валидации во второй вызов. Пустая очередь: `stream` → один `TextDelta("(fake)")`; `complete` → нейтральный `LLMResult(text="(fake)")`; `structured` → `LLMUnavailable("structured output failed")` (нет схемы, под которую можно синтезировать модель) — поведение и обоснование в докстринге класса. `status()` возвращает `self.forced_status` (по умолчанию `ok`). Проверено смоук-скриптом (временная папка, `uv run python`): `isinstance(FakeLLMClient(), LLMLike) is True`; `complete`/`stream`/`Exception`/пустая очередь — по списку задачи; `structured` на очереди из двух `LLMResult` (невалидный ограждённый JSON → валидный) вернул модель, `calls[1].messages` содержит сообщение о провале валидации; два невалидных подряд → `LLMUnavailable`. `uv run ruff check .` и `uv run ruff format --check .` чисто. |
| Промпты — загрузчик и файлы (§4.2 + §5.6) | сделан | 18.09.2026 | `app/llm/prompts.py`: `Prompt(name, version, text)`, `load_prompt(name)` ищет `<name>_v<N>.md` в `app/agents/prompts/` (путь — от `Path(__file__)`, не от cwd), берёт максимальный `N`, нет файла → `FileNotFoundError` с именем и путём папки; `Prompt.render(**vars)` — подстановка `{{var}}`, незаменённая переменная → `KeyError` с её именем; лишние переданные переменные, которых нет в тексте, — принимаются и не используются (задокументировано в докстринге модуля). `extractor_version` = `f"{name}_v{version}"`. Четыре промпта `app/agents/prompts/{selection,tutor,observer,extract_program}_v1.md`, все v1, на русском, по содержанию из product-logic §3.2, memory-architecture §9.4/§9.5/§8.1, product-logic §5.1. Плейсхолдер в `extract_program_v1` — `{{page_text}}` (текст страницы программы), выбран самостоятельно. Правка `@tool(name, description, read_only: bool = True)` — прокидывается в `ToolSpec.read_only`; расхождение с буквальной сигнатурой ТЗ §4.1 (там только `name`/`description`), разрешено в пользу §5.2 (`save_program` требует `read_only=False`), отмечено в `docs/sync-log.md`. Проверено: `uv run ruff check .` и `uv run ruff format --check .` чисто; смоук-скрипт во временной папке (`uv run python`, вне репозитория) — `load_prompt` для всех четырёх имён, `version == 1`, текст непустой; `observer.extractor_version == "observer_v1"`; `render()` без аргументов у `selection_v1`/`tutor_v1` проходит без `KeyError` и без остаточных `{{...}}`; `observer_v1.render()` со всеми пятью переменными убирает все `{{...}}`, без любой из пяти — `KeyError` с её именем; `load_prompt("несуществующий")` → `FileNotFoundError`; временные `x_v1.md`/`x_v2.md`, положенные прямо в `app/agents/prompts/` (загрузчик жёстко привязан к этой папке), дали `load_prompt("x").version == 2` и удалены после проверки — `git status` чист; `@tool(..., read_only=False)` → `ToolSpec.read_only is False`, `ToolRegistry(read_only=True).register(...)` на такой спеке → `ValueError`; `import app.llm.prompts` не тянет `app.agents.router` в `sys.modules`. |
| Router — run_chat, эхо (§5.1) | сделан | 18.09.2026 | `app/agents/router.py`: `AgentDeps(llm: LLMLike, pg: async_sessionmaker[AsyncSession], graph: AsyncDriver, redis: Redis)` — тот класс, который `app/llm/tools.py` ждёт отложенным импортом под `TYPE_CHECKING`; `run_chat(kind, ctx, message, deps)` — эхо: два сообщения (`system` из `load_prompt("selection"\|"tutor")` по карте `kind→имя промпта`, `user` из `message.text`), `deps.llm.stream(messages, "chat")` без `tools`, `TextDelta` наружу как есть, в конце `Done(event_id=0, mode=None, gave_task_instance_id=None, hint_level=None, referenced_skill_ids=[])`. `LLMUnavailable` до первого отданного события — пробрасывается исключением; после первого события — последним идёт `StreamError(code="llm_unavailable")` (сознательно не то поведение, что в `loop.py`, отмечено в докстринге). Явная пометка в докстринге про ветвление на `selection.run`/`tutor.run` и подмешивание истории через `db.repo.messages.list_messages` в фазе 2. Проверено смоук-скриптом (временная папка, `uv run python`, `FakeLLMClient`): kind `"selection"` → `TextDelta` в порядке, затем `Done(referenced_skill_ids=[])`, у первого вызова `calls[0].messages[0].role == "system"`, `calls[0].slot == "chat"`, `calls[0].tools is None`; kind `"prep"` → системный промпт равен тексту `load_prompt("tutor").text`, не равен `load_prompt("selection").text`; `LLMUnavailable` первым в очереди — пробрасывается исключением из `run_chat` (не событием); `LLMUnavailable` вторым (после одного `TextDelta`) — последним событием идёт `StreamError`; `typing.get_type_hints(ToolCtx)` (без `localns`) на самом деле падает с `NameError`: аннотация `deps: AgentDeps` резолвится в пространстве имён модуля `app.llm.tools`, а там `AgentDeps` есть только под `TYPE_CHECKING` (рантайм-импорта нет). Она разрешается в класс, только если явно передать `localns={"AgentDeps": AgentDeps}`. На работу это не влияет: декоратор `tool()` вычисляет аннотации функции-инструмента в её собственном модуле (`inspect.signature(fn, eval_str=True)`), где `ToolCtx` импортирован по-настоящему, а не в `app.llm.tools`. Убирать `TYPE_CHECKING` у `AgentDeps` нельзя — рантайм-импорт `app.agents` из `app.llm` перевернёт слои (`app.agents` зависит от `app.llm`, не наоборот). `uv run ruff check .` и `uv run ruff format --check .` чисто. |
| Реестр инструментов (§4.1) | сделан | 18.09.2026 | `app/llm/tools.py`: `ToolCtx(student_id, deps, request_id)` — `deps: AgentDeps` типизирован отложенной аннотацией и импортом под `TYPE_CHECKING` из `app.agents.router` (каталог `app/agents/` ещё не создан — правило соблюдено, импорт в рантайме отсутствует). `@tool(name, description)` проверяет сигнатуру (ровно два параметра, первый — подкласс `BaseModel`) в момент декорирования (`inspect.signature(fn, eval_str=True)`) и возвращает `ToolSpec(name, description, args_model, fn, read_only=True)`; нарушение → `TypeError` при импорте модуля. `ToolRegistry(read_only=False)`: `register` (дубль имени → `ValueError`; `read_only=True`-реестр отклоняет `ToolSpec(read_only=False)` → `ValueError`), `schemas()` (OpenAI-формат, пустой реестр → `[]`), `call(name, args, ctx)` (неизвестное имя → `KeyError`; невалидные аргументы → `ValidationFailed` из `app/errors.py`; результат-`BaseModel` сериализуется `model_dump()`), `names()`. Тела инструментов исключений не перехватывают — пробрасываются как есть. Проверено тем же смоук-скриптом: `echo`-инструмент (`schemas()[0]["function"]["name"]`, `parameters` с описаниями полей), `call` валидный/невалидный/неизвестный, дубль регистрации, отказ read_only-реестра, `TypeError` на функции с одним параметром и без аннотации `BaseModel`, сериализация Pydantic-результата в `dict`; `"app.agents" not in sys.modules` после `import app.llm.tools` — подтверждено. |
| Реестры агентов — selection/tutor/jobs стабы (§5.2–5.3 + §5.5) | сделан | 18.09.2026 | `app/agents/selection.py`: `run(ctx, message, history, profile, deps)` — стаб `NotImplementedError("phase 2")`, параметры типизированы существующими контрактами (`ChatCtx`, `ChatMessageIn`, `list[MessageOut]`, `Profile` из `app.schemas.profile`, `AgentDeps`). `SELECTION_TOOLS = ToolRegistry()` — восемь инструментов tech-stack §4.3 (`update_profile`, `run_matching`, `compare`, `save_program`, `get_program_facts`, `get_admission_route`, `get_exam_format`, `query_dataset`), у каждого своя Pydantic-модель аргументов с `Field(description=...)` по каждому полю; `save_program` — `read_only=False`, остальные семь — `read_only=True`; все тела — `NotImplementedError("phase 2")`. `app/agents/tutor.py`: `run(...)` — тот же стаб; `TUTOR_TOOLS = ToolRegistry(read_only=True)` с `explain_belief`, `get_task` и переиспользованной спецификацией `get_exam_format` из `selection.py` (решение и обоснование — в докстринге модуля: один и тот же факт об экзамене, `ToolSpec` — общий неизменяемый объект, независимость реестров не страдает от совместной регистрации). `app/agents/jobs.py` — шесть стабов ARQ-функций (`observe_chat`, `pregenerate_set`, `set_summary`, `propose_personal_nodes`, `soft_match`, `extract_program`) с сигнатурой `(ctx: dict, request_id: str, <именованные параметры>)` по ТЗ §5.5 и докстрингом, называющим очередь, триггер и таймаут из tech-stack §2.5; тело каждой — `NotImplementedError("phase 2")`. Проверено смоук-скриптом (временная папка, `uv run python`, вне репозитория): `SELECTION_TOOLS.names()` — ровно восемь ожидаемых имён, `schemas()` строится без исключений, ни одно `description` не пусто, у каждой схемы в `parameters.properties` есть `description` на каждое поле; `TUTOR_TOOLS.names()` — ровно три имени (`explain_belief`, `get_task`, `get_exam_format`); `TUTOR_TOOLS.register(selection.save_program)` → `ValueError`; вызов любого из восьми/трёх стабов через `registry.call(name, {...}, ctx)` → `NotImplementedError` наружу (не `ToolResult`); все шесть функций `jobs.py` импортируются, у каждой непустой докстринг с явным упоминанием таймаута. `uv run ruff check .` и `uv run ruff format --check .` чисто. Расхождения ТЗ с соседними документами — в `docs/sync-log.md`. |
| Observer — схема выхода (§5.4) | сделан | 18.09.2026 | `app/agents/observer.py`: `ObservationKind` — девять значений §5.4 буквально; `Observation` поле в поле по §5.4 (`ErrorClass` — импорт из `app/schemas/common.py`, не свой литерал); `ObservationOut` с единственным полем `observations`. `model_validator(mode="after")` сверяет обязательные поля по `kind` по таблице из `observer_v1.md` (источник истины по заданию шага — промпт, а не только неполный список примеров ТЗ §5.4): `solution_step`→outcome+skill_id, `task_in_chat`→instance_id+answer, `applied`/`confusion`/`question`→skill_id, `avoided_trap`→skill_id+misconception_id, `root_hint`→skill_id+root_skill_id, `proposed_misconception`→name+description+error_class, `pace_signal`→signal; сообщение об ошибке называет kind и недостающие поля (идёт в повторный запрос через `structured()`). `confidence` получил `default=1.0` (в буквальной сигнатуре ТЗ — обязательное поле) — пример §8.1 (`pace_signal`) не содержит `confidence`, а должен валидироваться целиком без правок; расхождение и обоснование дефолта — в `docs/sync-log.md`. `observe(client: LLMLike, window, context) -> ObservationOut` — стаб `NotImplementedError("phase 2")`, докстринг описывает будущую загрузку `observer_v1`, подстановку окна и списков, слот `bulk` в `structured()`, фильтрацию по `observer_min_confidence`. Проверено смоук-скриптом (временная папка, `uv run python`): пример из memory-architecture §8.1 (все 10 наблюдений, включая `pace_signal` без `confidence`) валидируется целиком, `len(observations) == 10`; `solution_step` без `skill_id`, `task_in_chat` без `instance_id`, `root_hint` без `root_skill_id`, `proposed_misconception` без `error_class` — каждый даёт `ValidationError` с текстом, называющим kind и поле; `confidence=1.2` и `summary` длиной 301 — каждый даёт `ValidationError`; по одному валидному примеру на все девять `kind` проходят. `uv run ruff check .` и `uv run ruff format --check .` чисто. |
| Цикл инструментов — run_tool_loop (§4.3) | сделан | 18.09.2026 | `app/llm/loop.py`: `LoopEnd` (dataclass, не `Pydantic`/не в `schemas/`, поля `text_full`, `tool_results`, `steps`) — имя закрывает расхождение ТЗ §4.3 п.3 (`LoopDone`/`LoopEnd`), запись в `docs/sync-log.md`. `run_tool_loop(client: LLMLike, registry, messages, slot, ctx, max_steps=6) -> AsyncIterator[StreamEvent \| LoopEnd]`: работает с локальной копией `messages` (`list(messages)`), не мутирует вызывающего; `tools=registry.schemas() or None`; `TextDelta`/`ToolCall` — наружу и накапливаются (текст / список вызовов шага), прочие события — наружу как есть; нет вызовов → `LoopEnd` и выход; есть вызовы → `registry.call` на каждый (любое исключение → `ToolResult(error=str(exc))`, цикл не прерывается), `ToolResult` наружу и в накопитель, в `messages` дописываются `assistant(tool_calls=...)` и по `tool(tool_call_id, content=json)` на результат (`json.dumps(..., default=str)` — не падает на несериализуемом); `steps > max_steps` → `StreamError(code="tool_loop_limit")`, следующий запрос к модели не делается; `LLMUnavailable` из `client.stream` на любом шаге (включая до первого токена) → `StreamError(code="llm_unavailable")`. Проверено смоук-скриптом (временная папка, `uv run python`, `FakeLLMClient` + реестр из одного `echo`): (а) 2-шаговый скрипт → события `ToolCall, ToolResult(data={"echo":"hi"}), TextDelta, LoopEnd`, `client.calls[1].messages` содержит `assistant` с `tool_calls` и `tool` с `tool_call_id="c1"`; (б) неизвестное имя инструмента → `ToolResult(error=...)`, цикл продолжается; (в) невалидные аргументы (`ValidationFailed`) → `ToolResult(error=...)`, не исключение наружу; (г) 7-шаговый скрипт вызовов при `max_steps=6` → последнее событие `StreamError(code="tool_loop_limit")`, `len(client.calls) == 6` (седьмого запроса нет); (д) `LLMUnavailable` в очереди → `StreamError(code="llm_unavailable")`; (е) пустой реестр → `client.calls[0].tools is None`; (ж) `LoopEnd` несёт накопленный текст, список результатов, число шагов; (з) исходный список `messages` не изменился (`before == after`, длина 1). `uv run ruff check .` и `uv run ruff format --check .` чисто. |
| Postcheck (§4.4) | сделан | 18.09.2026 | `app/agents/postcheck.py`: `check_facts(text, tool_results) -> PostcheckResult(ok, mismatches)`, чистый модуль (только `re`, `datetime.date`, `pydantic`, `app.schemas.chat.ToolResult` — без `app.db`/`app.graph`/`app.llm`/`sqlalchemy`/`neo4j`/`redis`/`openai`). Числа: целые/десятичные, пробел как разделитель тысяч, проценты, валюта рядом — сравнение с точностью до форматирования; голые числа 2024–2030 без группировки/десятичной части/процента не проверяются (учитывается только отдельно стоящее число, не год внутри распознанной даты). Даты — четыре вида по §4.4 (`12 мая 2027`, `12.05.2027`, `2027-05-12`, `15 декабря`); без года — сравнение по дню и месяцу; русские месяцы — родительный падеж (после числа дня) и именительный, оба в одном словаре, но именительная форма участвует только внутри шаблона «число + месяц» (голое «май» в прозе датой не считается — докстринг объясняет выбор). Из `tool_results` числовые и датовые значения собираются рекурсивно на любую глубину (словари и списки), строки, парсящиеся как ISO-дата, нормализуются в `date`. Пустой `tool_results` при числах в тексте → `ok=False` — не отдельная ветка, а естественное следствие того, что число не находится среди (пустого) множества результатов. Проверено смоук-скриптом (временная папка, `uv run python`): `"Стоимость 1 500 CHF в год"` против `{"tuition_per_year": 1500}` → ok; `1450` вместо `1500` → `mismatches == ["1450"]`; `"подача до 15 декабря 2026"` против `{"date": "2026-12-15"}` → ok, против `"2026-12-01"` → не ok; `"в 2027 году"` без результатов → ok; текст без чисел/дат с пустыми результатами → ok; число при пустых результатах → не ok; число внутри списка внутри словаря — находится. Отдельным процессом (`uv run python`, `sys.modules`) подтверждено: импорт `app.agents.postcheck` не тянет `app.llm`, `sqlalchemy`, `neo4j`, `redis`. `uv run ruff check .` и `uv run ruff format --check .` чисто. |
| Выбор провайдера, docs/decisions/llm-provider.md (§2) | сделан с оговоркой | 18.09.2026 | Документ перенесён из неположенного `docs/tz/provider.md` в `docs/decisions/llm-provider.md` (именно перенос, не копия) и доведён до приёмки: `scripts/llm_probe.py` написан и прогнан на живом ключе трижды (дважды `response_format`, один раз `LLM_STRUCTURED_MODE=tool` для чек-листа §6 п.3) — реальным `LLMClient`/`Settings`, без моков. Результаты вписаны в таблицы «Результаты пробы»: `structured(ObservationOut)` — 2 из 3 валидны с первого раза в обоих режимах, все `skill_id` из переданного списка (критерий §2 пройден); `stream()` с `get_time` — инструмент вызывается и текст после него есть, но время до первого `text_delta` 12.98–14.59 с при критерии < 3 с (**не пройден**, задокументировано как открытый риск на Ф2, не блокирует Ф1 — эхо-режим инструменты не использует). Ни разу не упало на 400, лестница response_format→tool из provider.md не понадобилась (`tool`-режим всё равно проверен отдельно). **Честно непроверено:** лимиты аккаунта (запросов/мин, токенов/день) — нет доступа к дашборду Together из этой сессии, `LLM_RPM_CHAT`/`LLM_RPM_BULK` оставлены на консервативной заглушке `10` вместо «80 % от подтверждённого лимита»; остаток кредитов на ключе тоже не проверен. `docs/tz/checklists/phase1-B2.md` заведён (см. отдельную строку ниже). `deploy/.env.example` дополнен блоком `LLM_*`/`MODEL_*` (запись в `docs/sync-log.md`). |
| Чек-лист приёмки — docs/tz/checklists/phase1-B2.md (§6, семь пунктов) | сделан с оговоркой | 18.09.2026 | Таблица «пункт / как проверяю / результат / дата» по семи пунктам §6 дословно. Пункты 1–3 закрыты результатами пробы из строки выше (1–2 пройдены, время до первого `text_delta` в п.2/3 — нет). Пункт 4 (20 вызовов `bulk` без 429) сознательно отложен — вне «ровно тех вызовов, что описаны в §2», и тестировать `LLM_RPM_*`, которые сами не подтверждены дашбордом, преждевременно; причина в таблице. Пункты 5–7 (сеть вниз/вверх → `/health`; `LLM_FORCE_DOWN=1` → `/health` + 503; `curl -N` эхо-чат) — заблокированы: `app/api/`, `app/main.py` в этой фазе пустые заглушки B3, эндпоинтов `/health` и `/chat/{kind}/messages` не существует. Запись в `docs/sync-log.md`. |
| Правка confidence в observer.py — уточнение решения §5.4 | сделан | 18.09.2026 | Сужена запись из `docs/sync-log.md` от того же дня: раньше `confidence: float = Field(default=1.0, ...)` был общим дефолтом для всех девяти `kind`, из-за чего забытый моделью `confidence` у любого вида (включая `solution_step`) молча становился максимальным и проходил `observer_min_confidence` — против правила §8.1 «при сомнении низкий confidence, а не пропуск» (и буквально того же текста в `observer_v1.md`). Теперь `confidence: float | None = Field(default=None, ge=0, le=1)`, и `model_validator` требует его явно для всех `kind`, кроме `pace_signal` — для него отсутствие остаётся допустимым (в `Evidence` `pace_signal` не превращается, вес/ярус «—» по таблице §8.1). Проверено смоуком: пример §8.1 целиком по-прежнему валидируется (10/10 наблюдений, `pace_signal` без `confidence` проходит); `solution_step` без `confidence` → `ValidationError` с текстом, называющим `confidence` среди недостающих полей. `uv run ruff check .` / `uv run ruff format --check .` чисто. |
| `Settings` читает `.env` — env_file в config.py (не по ТЗ 30-B2, по прямому поручению шага) | сделан | 18.09.2026 | `app/config.py` — чужой файл (00-contracts §2: B3), правка по прямому поручению: `SettingsConfigDict` не задавал `env_file`, поэтому `Settings()` рядом с заполненным `backend/.env` отдавала пустые `LLM_BASE_URL`/`LLM_API_KEY`/`MODEL_CHAT`. Добавлены `env_file=".env"`, `env_file_encoding="utf-8"`, `extra="ignore"` (без `extra="ignore"` лишние переменные `.env`, которых нет среди полей `Settings`, роняли бы валидацию — по умолчанию `pydantic-settings` `extra="forbid"`, проверено явно). Приоритет переменных окружения над файлом — поведение библиотеки по умолчанию, проверено явно (`LLM_STRUCTURED_MODE` из окружения перекрывает значение из `.env`). Больше в файле не менялось. Запись в `docs/sync-log.md`, ревью на синке B3. |
| Контракты — schemas/llm.py + дополнение chat.py/common.py/config.py по 00-contracts §6 (снимает временный шим) | сделан | 18.09.2026 | Строго по 00-contracts §6, без додумывания полей. Проверено: `uv run ruff check .` и `uv run ruff format --check .` чисто; импорт `from app.llm.client import LLMClient, LLMLike; from app.schemas.chat import StreamEvent; from app.schemas.llm import LLMResult` без ошибок; `grep _schemas_shim` по backend/ — ноль вхождений; смоук — `TextDelta(text="a")` и `Done(event_id=0, mode=None, gave_task_instance_id=None, hint_level=None, referenced_skill_ids=[])` собраны без явного `type` и провалидированы через `TypeAdapter(StreamEvent)` из `model_dump()` — дискриминатор по `type` отработал на обоих. Коммит `136383d` (только schemas/ и config.py), затем отдельным коммитом — переключение импортов в `client.py` и удаление шима. |

## Временные обходы

Шима больше нет: `app/schemas/llm.py` создан по `00-contracts.md` §6, `app/schemas/chat.py`
дополнен StreamEvent-вариантами. Импорты в `client.py` переключены на
`from app.schemas.llm import ...` / `from app.schemas.chat import ...`,
файл `app/llm/_schemas_shim.py` удалён. Записи в `docs/sync-log.md` закрыты.

**Исправление 18.09.2026 05:20**: до этого момента в сессии по ошибке был создан
параллельный каталог `apps/api/app/` (вместо настоящего `app/`) — шим,
локальный `.venv` (`openai==1.109.1`, `redis==5.3.1`) и фиктивные `Settings`/
`LLMUnavailable` лежали там, на неверной предпосылке «скелет B3 не смержен».
На деле `backend/app/` и `backend/.venv` — настоящий смерженный скелет
(Python 3.12.10, `uv`, зависимости уже установлены: `openai==3.14.1`,
`redis==5.3.1`, `pydantic==2.13.5`, `pydantic_settings==2.15.0`,
`fastapi==0.141.1`). `apps/api/` целиком удалён. `Settings`
(`app/config.py`) и `LLMUnavailable` (`app/errors.py`, `code="llm_unavailable"`,
`status=503`, наследник `AppError`) — настоящие, импортировать напрямую:
`from app.config import Settings`, `from app.errors import LLMUnavailable`.
Поля `Settings` — верхним регистром (`settings.MODEL_CHAT`, `settings.LLM_BASE_URL`
и т.д.), а не как в тексте ТЗ §3.2 (`settings.model_chat`) — расхождение ТЗ
с реальным контрактом, не устранено.

**Правка уже сданного `client.py` (18.09.2026, при реализации fake-клиента и реестра
инструментов)**: разбор structured-ответа (`_parse_structured`) и текст
сообщения о провале валидации вынесены из `LLMClient` в новый общий модуль
`app/llm/structured.py` (`parse_structured_output`, `structured_validation_retry_message`) —
чтобы `FakeLLMClient.structured()` воспроизводил тот же двухпопыточный
цикл разбора/повтора байт-в-байт, без копипасты. `LLMClient.structured()`
теперь вызывает эти функции вместо приватного метода; внешнее поведение
(порядок попыток, текст сообщения, условия `LLMUnavailable`) не менялось —
подтверждено смоук-скриптом на структурированном выводе (см. строку
«Клиент — structured output» выше и новую строку «Fake-клиент»).

## Что осталось до приёмки §7

- **Тесты §6** (`tests/llm`, `tests/agents`) — не пишутся в этой сессии по прямому
  указанию: раздел §6 вне скоупа этого шага, тесты — отдельная сессия per
  `docs/tz/30-B2.md` шапка/§6/00-contracts §8 п.1. Блокирует: `make test` /
  `make test-int` из критерия приёмки — их пока не от чего запускать зелёными.
- **Чек-лист §6, пункты 5–7** (сеть вниз/вверх → `/health`; `LLM_FORCE_DOWN=1` →
  `/health` + `POST /chat/.../messages` → 503; эхо-чат через `curl -N`) —
  заблокированы отсутствием слоя B3: `app/api/`, `app/main.py` в этой фазе пустые
  однострочные заглушки. Блокирует: приёмка требует «чек-лист заполнен, пункты
  1–2, 5–7 пройдены» — 5–7 физически невыполнимы до проводки B3.
- **Чек-лист §6, пункт 4** (20 вызовов `bulk` без 429) — сознательно отложен, не
  заблокирован: решение не тратить 20 платных вызовов на непроверенный
  `LLM_RPM_BULK`. Блокирует: ничего формально (не входит в список «1–2, 5–7» из
  §7), но стоит сделать после проверки лимитов аккаунта.
- **Лимиты аккаунта Together** (запросов/мин, токенов/день) — не проверены, нет
  доступа к дашборду из этой сессии; `LLM_RPM_CHAT`/`LLM_RPM_BULK=10` — временная
  консервативная заглушка, не «80 % от подтверждённого лимита». Блокирует:
  приёмка требует «лимиты с датой» в `docs/decisions/llm-provider.md` — сейчас
  там честно написано «не проверено», а не число с источником.
- **`stream()` время до первого `text_delta` при инструменте** — 12.98–14.59 с
  на живом прогоне при критерии < 3 с (`docs/decisions/llm-provider.md`,
  «Вердикт по критериям приёмки §2»). Не блокирует приёмку Ф1 напрямую (в §7 нет
  этого критерия буквально, только чек-лист §6 п.2, который отмечен «частично»),
  но открытый риск на Ф2 (`run_tool_loop` тутора) — не устранено, только
  зафиксировано.
- **Остаток кредитов на ключе и оценка расхода на хакатон** — не проверены/не
  посчитаны (см. `docs/decisions/llm-provider.md`, «Отклонение от бюджета»). Не
  блокирует §7 формально (§7 не требует этих чисел), но нужно раньше, чем
  начнётся боевой трафик Ф2–Ф3.
- **Коммит** — не делался в этой сессии (не просили). Все файлы этого шага живут
  только в рабочей копии; напоминание отдельным пунктом — `backend/docs/`
  целиком в `.gitignore` (см. `docs/sync-log.md`, запись `[B2 → B3]` про
  `.gitignore`), так что даже при коммите `docs/decisions/llm-provider.md` и
  этот файл не попадут в git без правки `.gitignore` кем-то из владельцев.
