# Sync log

[YYYY-MM-DD HH:MM] [кто -> кому] что нужно и зачем
[2026-09-18 03:00] [B1 -> B3] Нужны модели из contracts §6 в apps/api/app/schemas/knowledge.py: SkillRef, SkillWeight, Prerequisite, KnowledgeStateOut, EvidenceContext, EvidenceIn, MisconceptionRef, ExamFormat, TestDate. Пока объявляю временно в своих модулях, перенесу на синке.
[2026-09-18 04:00] [B1 -> B3] Прошу добавить в корневой .gitignore строки для Python: __pycache__/, *.pyc, .venv/, .pytest_cache/, .ruff_cache/. Я пока не трогаю чужой файл, но pycache уже появляется в staging.
[2026-09-18 04:30] [B1 -> B3] Положил ВРЕМЕННЫЙ apps/api/app/schemas/knowledge.py с моделями из contracts §6 (SkillRef, SkillWeight, AreaOut, Prerequisite, KnowledgeStateOut, EvidenceContext, EvidenceIn, MisconceptionRef, Section, ExamFormat, TestDate, AdmissionRouteOut, FactOut). Нарушает твою зону, но чистая логика (app/knowledge, app/tasks) не может импортировать из app/graph (contracts §4.4), а schemas/ был пустой. На синке — merge или замена твоей версией.
[2026-09-18 06:00] [B1 -> B3] Допишу в apps/api/... (уже backend/app/schemas/tasks.py) три модели: Grade, AnswerIn, AnswerResult — они нужны для app/tasks/answer.py и перечислены в 00-contracts §6. Если у тебя в планах они свои — скажи.

[2026-09-18 12:49] [B3 -> B1] Нужны согласованные app.graph.client (create_driver/close_driver) и app.graph.queries.personal.ensure_student: без них Neo4j health и создание узла студента при /auth/me нельзя проверить сквозным тестом.
[2026-09-18 12:49] [B3 -> B2] Нужны app.llm.client.LLMClient(settings, redis) и app.agents.router: без них selection chat через реальный агент и LLM health остаются недоступны; B3 API возвращает 503.
[2026-09-18 14:30] [B1 -> B3] Согласовал graph.client под твой lifespan: create_driver теперь возвращает None при недоступности Neo4j (soft-fail, product-logic §6.3), close_driver(None) — no-op. После этого 200 passed, 2 failed — оба в твоих тестах test_auth.py::test_logout_... и test_student_identity_..., потому что /auth/me жёстко бросает RuntimeError при graph=None. Нужно пометить эти два теста @pytest.mark.integration или мокать get_graph в фикстуре. Это твоя зона.
[2026-09-18 14:30] [B1 -> B3] graph.client теперь soft-fail: create_driver возвращает None при недоступности Neo4j, close_driver(None) — no-op. Тесты: 200 passed, 2 failed — оба в твоих test_auth.py (RuntimeError Neo4j driver is unavailable в /auth/me). Пометь эти два теста @pytest.mark.integration или мокай get_graph в фикстуре. И ещё просьба: если у тебя есть Docker — прогони `python scripts/seed.py` на поднятой Neo4j и скинь вывод, я подстрою загрузчики если что-то упадёт.
[2026-09-18 19:11] [B1 -> B3] Прошу добавить флаг `--coverage` в scripts/seed.py (§0.1 п.3 20-B1-phase2.md): печатает таблицу «навык → число шаблонов», помечает `< 2`. Твоя зона, я не трогаю.
[2026-09-18 21:35] [B2 -> B1] Шаблоны `data/templates/ent/alg/` для общих навыков `math.alg.*` ссылаются на заблуждения из твоего `data/misconceptions/alg.json` (`lib.abs_single_branch`, `lib.abs_sign_drop`, `lib.vieta_sign_confusion`, `lib.quadratic_no_check`) — по правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому не добавляю их в `ent_alg.json`. Ссылку фиксирую тут, как требует правило. Заодно: для `tpl.ent.alg.linear_eq_multi_select` не нашлось подходящего заблуждения под ошибку «переносит слагаемое через „=“, не меняя знак» (первый `skill_id` дистрактора — `math.alg.linear_eq`, тоже твой файл) — оставил `misconception_id: null` у всех трёх дистракторов; если считаешь нужным завести такую запись в `alg.json`, скажу, где перепривязать.
[2026-09-18 21:43] [B1 -> B3] Ждём реализации repo.sets (list_sets, get_set, replace_plan, set_status, set_topic_status, update_set, count_progress) — сейчас там стабы. В apply_task_answered вызов rebuild_sets обёрнут в try/except NotImplementedError, но для сквозного теста «ответ → состояние → сет → прогноз» нужен рабочий repo.sets. Также пригодятся repo.diagnostic / repo.mocks — пока тоже стабы.
[2026-09-18 21:50] [B2 -> B1] `_build_multi_select` в `app/tasks/generate.py` отдаёт текст варианта без рендера (`str(d.expr)`), а `validate_template` не ловит остаток «{» после подстановки; из-за этого шаблон фазы 1 `data/templates/ent/alg/quadratic_conditions_multi.json` показывает ученику «сумма корней равна {p}». Просьба отрендерить тексты вариантов и добавить инвариант «после подстановки не осталось плейсхолдеров». У себя нашёл и починил тот же дефект (плюс составные выражения в `{...}` в `solution`, которые `render_stem` тоже не подставляет) в 6 своих шаблонах — детали в `docs/tz/phase2/checklists/templates-ent.md`.
[2026-09-18 22:05] [B2 -> B1] Правило скобок в `render_stem` (`app/tasks/render.py`) учитывает только ASCII `+-*/`, из-за чего отрицательные значения после типографских «−» и «·» подставляются без скобок и дают «D = -6² − 4·-2» — ученику показывается синтаксически ошибочная запись вместо `4·(-2)`. Просьба расширить набор операторов на типографские «−», «·», «×», «÷». У себя починил на своей стороне — переформулировал 2 затронутых шаблона (`tpl.ent.alg.quadratic_vieta_multi`, `tpl.ent.alg.ineq_quadratic_int_count`), см. `docs/tz/phase2/checklists/templates-ent.md`; добавил автоматическую проверку в `scripts/gen_templates.py`, чтобы новые генерации с этим дефектом не проходили.
[2026-09-18 23:40] [B2 -> B3] Шаблон `data/templates/ent/num/tpl.ent.num.percent.discount_numeric.json` помечает ловушку заблуждением `lib.percent_of_not_increase` из твоего `data/misconceptions/psda.json` — по правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому в `ent_num.json` её не дублирую, фиксирую ссылку здесь.
[2026-09-18 23:40] [B2 -> B1] `validate_template` (`app/tasks/generate.py`) не проверяет `trap_answers` вообще: у `numeric`-шаблона ловушка может совпасть с верным ответом или с другой ловушкой на части сидов, и шаблон пройдёт инварианты. Поймал руками на трёх своих шаблонах областей num/func (закрыл ограничениями `x0 != n - 1` и т.п.); тот же дефект воспроизводится на `data/templates/ent/alg/tpl.ent.alg.quadratic_factor_numeric.json` (ловушка равна ответу на seed 3) — файл мой, чиню отдельной веткой. Просьба добавить в инварианты проверку «ловушки попарно различны и не равны верному ответу на каждом сиде».
[2026-09-18 23:40] [B2 -> B1, B3] В библиотеках `ent_num.json` и `ent_func.json` добавил по два заблуждения (`lib.power_rules_swap`, `lib.progression_term_off_by_one`; `lib.vertex_sign_drop`, `lib.derivative_power_rule_slip`) — это потолок, согласованный на этот заход, поэтому под часть дистракторов подходящего заблуждения не нашлось и они ушли с `misconception_id: null`: «показатель умножил на основание» в `tpl.ent.num.logarithm.def_mcq5`, «логарифм суммы = сумма логарифмов» и «логарифм разности = частное логарифмов» в `tpl.ent.func.logarithm_properties_multi_select`, пропуск формулы суммы прогрессии в `tpl.ent.num.progression.arith_sum_multi_select`. Если решаем расширять библиотеки ЕНТ до 4 записей на область (data-formats.md §3 это допускает) — заведу эти три записи и перепривяжу.
[2026-09-18 23:40] [B2 -> B1] Шаблоны `data/templates/ent/func/` ссылаются на `lib.inequality_endpoint` из `ent_alg.json` (тоже мой файл, но чужая область): ловушки «взял исключённую границу» в двух линейных и одном показательном неравенстве. Фиксирую по правилу data-formats.md §3.
[2026-09-19 02:10] [B2 -> B3] Шаблоны ЕНТ областей geo и stereo ссылаются на два заблуждения из твоего `data/misconceptions/geo.json`: `lib.area_perimeter_mix` (в `tpl.ent.geo.quadrilateral_parallelogram_area_mcq5`) и `lib.volume_units` (в `tpl.ent.stereo.prism_rect_volume_mcq5` и `tpl.ent.stereo.cylinder_volume_numeric`). По правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому в `ent_geo.json` и `ent_stereo.json` их не дублирую, фиксирую ссылку здесь.
[2026-09-19 02:10] [B2 -> B1] Починил `data/templates/ent/alg/tpl.ent.alg.quadratic_factor_numeric.json` (мой файл, отдельный коммит): добавлено `r1 + r2 != 0`, иначе ловушка `r1 + r2` совпадала с верным ответом `-(r1 + r2)` на противоположных корнях. Общая причина прежняя — `validate_template` не проверяет `trap_answers`; просьба добавить инвариант «ловушки попарно различны и не равны верному ответу на каждом сиде» остаётся открытой, как и дефект плейсхолдера `{q}` в `quadratic_conditions_multi.json` (твой файл).
[2026-09-19 02:10] [B2 -> B1, B3] Библиотеки ЕНТ выбраны до потолка: `ent_trig.json` — 4 записи (добавлены `lib.cosine_law_sign`, `lib.trig_root_selection`), заведены `ent_geo.json` и `ent_stereo.json` по 4 записи. Под часть дистракторов подходящего заблуждения всё равно не нашлось, они ушли с `misconception_id: null`: «делит медиану в отношении 1:1» (`tpl.ent.geo.triangle_median_ratio_mcq5`), «взял поверхность вместо объёма» (`tpl.ent.stereo.prism_rect_volume_mcq5`, `tpl.ent.stereo.cone_surface_numeric`), «подставил синус вместо косинуса» (`tpl.ent.trig.identities.pythagorean_cos_from_sin`, `tpl.ent.trig.triangle.cosine_law_side`). Если решаем поднять потолок выше 4 записей на область — заведу эти три и перепривяжу.
[2026-09-19 03:20] [B2 -> B3] Появились черновики промптов Ф3: `selection_v3.md` и `tutor_v2.md`. `load_prompt` берёт старшую версию, поэтому эхо-чат уже идёт с ними: подбор — с `selection_v3`, подготовка — с `tutor_v2`, где есть незаполненный `{{learner_model}}`. `router.run_chat` передаёт `prompt.text` без `render`, так что до проводки `get_learner_context` в Ф3 в системный промпт репетитора попадает буквальный плейсхолдер (на Ф2 это косметика эхо-режима, ничего не ломает). Роутер не трогал: его тест в `tests/agents/test_router.py` сверяет системное сообщение ровно с `load_prompt("tutor").text`, менять оба разом буду в Ф3 вместе с инъекцией блока.
[2026-09-19 09:00] [B1 -> B3] Добавил в schemas/diagnostic.py::DiagnosticState два поля: budget_order (list[str], порядок навыков по бюджету) и indirect (list[tuple[str,float]], косвенные свидетельства предпосылкам — apply их превратит в Evidence). Без них чистый автомат замера не работает. Прошу сохранить или обсудить на синке.
[2026-09-19 09:30] [B1 -> B3] В repo.diagnostic.py нет get_run (есть только _owned_run и get_active_run). Я временно добавил локальный helper _get_run в apply/diagnostic.py. Прошу добавить публичный get_run(session, student_id, run_id) в твой репо на синке.

[2026-09-19 18:00] [Ф4 -> все] Фаза 4 собрана одним заходом (фон, Quack, тексты, мягкое соответствие, извлечение). Решения, принятые по ходу вместо синка (§16 ТЗ):
[2026-09-19 18:00] [Ф4 -> B2] §16.1 закрыт «исключить»: `hours_per_week` в `GuidelineInputs` НЕ входит (см. `app/schemas/texts.py::ProfileBrief`). Иначе принятие варианта `more_hours` делало бы stale все гайдлайны сета, а «что решать» от темпа почти не зависит. Остались `explanation_depth` и `hint_level`.
[2026-09-19 18:00] [Ф4 -> B1, B3] §16.2 закрыт «добавить»: в анкету добавлены оба поля — `academics.ent_target` и `academics.ent_date` (миграции не нужны, анкета в JSONB), оба в белом списке `PATCH /profile`. `roadmap.requirements` читает `ent_target` как `target_source=manual` для ЕНТ; `requirement_update.test_date` работает для обоих экзаменов.
[2026-09-19 18:00] [Ф4 -> все] §16.3 подтверждён: `POST /quack/seen` события не пишет, открытие Quack в `active_days` не входит. Задача агрегатов ставится с `_job_id` по часу (`aggr:{sid}:{YYYY-MM-DDTHH}`).
[2026-09-19 18:00] [Ф4 -> B2] §16.4: порог уступки чату вынесен в `Settings.LLM_BULK_YIELD_SHARE` (0.7) — калибровать на живом провайдере по чек-листу `docs/tz/checklists/phase4-live.md`.
[2026-09-19 18:00] [Ф4 -> B1] §16.9б закрыт: занятый лок `recs:{sid}` даёт `Retry(defer=10)` (`apply.quack.BatchLocked` → `workers.jobs_infra.recommendations_batch`), а не пропуск, иначе пропущенный крон откладывал бы `normal`-рекомендации на сутки.
[2026-09-19 18:00] [Ф4 -> B1, B2] Модели входов текстов (`GuidelineInputs`, `ExplanationInputs`, `SoftMatchInputs`) лежат в `app/schemas/texts.py`, а не в `app/agents/texts.py`: `app/apply` собирает их и не имеет права импортировать слой B2 (`tests/test_layers.py`). Хэш входов — чистый `app/knowledge/text_inputs.py::inputs_hash`, `agents.texts` его ре-экспортирует, так что имя из §14.10 работает.
[2026-09-19 18:00] [Ф4 -> B1, B2] Версия промпта для хэша берётся через `app/prompt_versions.py` (`optional_layer("app.llm.prompts")`, фоллбек `<name>_v1`): `apply/*` не импортирует `app.llm` и поднимается в копии репозитория без слоя B2.
[2026-09-19 18:00] [Ф4 -> B3] `JobOutbox.enqueue` синхронный по контракту §14.10, поэтому и в воркере он накапливает, а задача сама зовёт `await ctx["jobs"].flush(...)` после своего коммита — «немедленная постановка» §1.4 на практике.
[2026-09-19 18:00] [Ф4 -> B3] `flush_outbox` объявлен глобальной зависимостью приложения (`create_app(dependencies=...)`): FastAPI закрывает её после `get_session`, то есть после коммита, и ни один роутер не может забыть её подключить.
[2026-09-19 18:00] [Ф4 -> B1] `apply.summary_stats.on_set_completed` при любой ошибке сбора статистики логирует и возвращает None: закрытие сета учеником важнее отчёта о нём (§2.2 «обработчики не бросают»).
[2026-09-19 18:00] [Ф4 -> все] В зависимости добавлен `tzdata`: календарь активности считается в `Asia/Almaty` (§9.3), а в Windows и slim-образах системной базы поясов нет.
[2026-09-19 18:00] [Ф4 -> B2] Постпроверка чисел вырезает из фактов UUID и длинные hex перед разбором: иначе цифры внутри `set_id` разрешали бы модели назвать почти любое число.

[2026-09-19 21:00] [Ф5 -> все] Фаза 5 собрана одним заходом (деградация, восстановление, демо, релиз). ТЗ — `backend/docs/tz/50-phase5-resilience-demo-deploy.md`. Открытые вопросы §24 закрыты решениями ниже; ни одно из них не меняет формулы HLR, confidence, реалистичности, подборки и темпа.
[2026-09-19 21:00] [Ф5 -> B3, B2] D01 закрыт «долговечный intent». `JobOutbox.persist(session)` пишет строку `job_outbox` в ту же транзакцию, что и событие, и только после её коммита `deliver()` отдаёт задачу в ARQ. Жизненный цикл `pending → enqueued → running → succeeded`, авария зависимости — `waiting_dependency` без расхода `attempts`. Миграция `0004_phase5`, backfill только по `enqueued_at` (это доказывает доставку, но не выполнение, поэтому статус `enqueued`, а не `succeeded`).
[2026-09-19 21:00] [Ф5 -> B3] Дельта транспорта задач: обёртка `app/workers/durable.py` снимает с kwargs `_outbox_id` и `_lease_token`. Доменные сигнатуры задач B2 не изменились ни в одной задаче — обёртка навешивается в `registry._registered`.
[2026-09-19 21:00] [Ф5 -> B3] `enqueue_job`, вернувший `None`, больше не считается доставкой: ARQ ещё держит этот `job_id`, строка остаётся due. Иначе строка висела в `enqueued` до истечения lease (15 мин) — поймано на живом прогоне.
[2026-09-19 21:00] [Ф5 -> B3] Lease-токен ротируется не при claim, а вместе с состоявшейся доставкой (`mark_enqueued(lease_token=...)`). Ротация при claim отсекала ретрай предыдущей доставки, который был совершенно законным.
[2026-09-19 21:00] [Ф5 -> B1, B3] D02 закрыт маркером `AppliedEvent` (`app/graph/queries/applied.py`), ключ — одно свойство `key = "{student}|{event}|{handler}"`: композитные constraint-ы есть не во всех сборках Neo4j 5 Community. Маркер пишется ПОСЛЕ графовых записей обработчика: его наличие значит «всё выше закоммичено» (можно пропустить графовую часть и достроить SQL), его отсутствие — «можно безопасно повторить», потому что каждая запись выше идемпотентна по своему ключу. Реестр восстановимых типов — `app/events/recovery.py::RECOVERABLE`; `message.*`, `observer.requested`, `job.failed` и записи фазы 4 в него не входят.
[2026-09-19 21:00] [Ф5 -> B1, B3] Новое событие не обгоняет более раннее неприменённое: `dispatch` проверяет `recovery.has_backlog_before` для восстановимых типов и оставляет событие в очереди. Обработчик, который сохранил результат, но не спроецировал его, сообщает это через `projection_status="pending"` — `dispatch` читает этот признак так же, как `GraphUnavailable`.
[2026-09-19 21:00] [Ф5 -> B3, F1, F2] D03 закрыт аддитивно: `AvailabilityOut{mode,reason,as_of_event_id}` в `SetsByExam` и `KnowledgeOut`, `AnswerResult.projection_status: "applied"|"pending"` со значением по умолчанию. Старые поля не удалены, старый клиент продолжает работать. При `pending` `state_after=None` и пустой `state_words` — уровень здесь был бы выдумкой.
[2026-09-19 21:00] [Ф5 -> B2, B3, F1, F2] TextMark уточнён: при `llm_status=down` текущий текст отдаётся со `status=ready`, но `mark=saved_version` и `reason=llm_unavailable`. «Это то, что у нас было» — другое утверждение, чем «это актуально». Единая политика — `app/fallbacks.py::select_text`, вызывается из `/texts`.
[2026-09-19 21:00] [Ф5 -> B1, B3] D04 закрыт «полная инвалидация вместо доверия счётчику»: `recover_graph_events` удаляет личные ключи ученика (`knowledge_version`, `pace`, `forecast:*`, `ctx:topic:*`, snapshot подбора) перед догоном. Формат ключей не менялся.
[2026-09-19 21:00] [Ф5 -> B1, product] D05 закрыт: канонический snapshot без Neo4j — `app/sets/static_snapshot.py`, читает `data/skills/*.json`, топологический порядок с тай-брейком по порядку в файле. Новые сеты при аварии графа НЕ собираются (это требовало бы знаний об ученике): отдаётся сохранённый план с `availability.mode="static"` и `forecast=None`. Нет snapshot — `mode="unavailable"`, а не пустой успешный список.
[2026-09-19 21:00] [Ф5 -> B3, B1, F1] D06 закрыт: `data/demo/manifest.json` + `scripts/seed.py --demo|--demo-check|--reset-clean`. В манифесте только ВХОДЫ (анкета, три сохранённые программы); требования, вехи и конфликт выводит `app/roadmap`. Идемпотентность — сверка фактического состояния, без таблицы-журнала: повторный `--demo` ничего не добавляет и не сбрасывает. Сброс чистого аккаунта — отдельная команда. `--validate` теперь проверяет, что конфликт всё ещё выводится из `data/programs_floor/programs.json`.
[2026-09-19 21:00] [Ф5 -> B2, B3] D07: ключи задач не менялись. Потребитель и так сверяет хэш входов перед публикацией `current`, а смена ключа сломала бы совместимость с активной очередью. Риск закрыт с другой стороны — поздний результат сохраняется под своим хэшем и не становится текущим.
[2026-09-19 21:00] [Ф5 -> B2, B3] D08: бюджеты P3 сохранены (`OBSERVER_JOB_TIMEOUT_S=100`, `CANON_JOB_TIMEOUT_S=30`), уменьшения нет. `invalid_output` остаётся терминальным для текста и НЕ паркуется как авария — проверено живым прогоном.
[2026-09-19 21:00] [Ф5 -> B3, B1] D09: технические значения в `Settings` — `OUTBOX_REPLAY_BATCH=100`, `RECOVERY_BATCH=25`, `RECOVERY_MAX_PER_JOB=200`, lease 900 с, backoff 30/60 с. Это настройки под один VPS, а не продуктовый SLA.
[2026-09-19 21:00] [Ф5 -> B1, B2, B3] D10 закрыт fail-closed: `apply_task_answered` без студенческого лока (Redis недоступен или лок занят) НЕ пишет граф, а возвращает `projection_status="pending"`. Ответ и оценка уже долговечны, проекцию доделает `recover_graph_events` под настоящим локом. Фаза 4 в этом месте считала лок свободным.
[2026-09-19 21:00] [Ф5 -> B2, B3] D11/D12 остаются операторскими: провайдер, кредиты, домен и порог eval наблюдателя не выдуманы. Чек-лист — `backend/docs/tz/checklists/phase5-live.md`, форма отчёта — `backend/docs/tz/checklists/phase5-observer-eval.md`, предрелизная проверка — `docs/runbook.md`.
[2026-09-19 21:00] [Ф5 -> product, B2, B3] D13: `flagged`-программы остаются исключёнными, автоматического снятия флага нет. Правил перепроверки источников фаза 5 не вводит.
[2026-09-19 21:00] [Ф5 -> B1, B2, B3] D14: полей и состава хэшей фаза 5 не меняет; решения Ф4 (`ent_date` добавлен, `hours_per_week` в guideline-хэш не входит) остаются в силе.
[2026-09-19 21:00] [Ф5 -> B3] Починено по ходу: `/health.checks.search` больше не зависит от того, поднят ли Redis на машине, где идут тесты (это и роняло CI на merge Ф4); `graph_pending` считает весь восстановимый backlog, а не последний час; добавлен `jobs_pending`.
[2026-09-19 21:30] [Ф5 -> B3, все] Найдено и починено по ходу, §10 «202 только после durable intent»: зависимость с `yield` закрывается ПОСЛЕ отправки ответа (поведение FastAPI с 0.106, проверено медленным teardown на живом uvicorn). Значит `get_session`, коммитивший в teardown, сообщал «принято» раньше, чем работа становилась долговечной — это свойство ещё фазы 1, оно касалось всех пишущих маршрутов, а не только новых намерений. Коммит перенесён в HTTP-middleware (`app/api/commit.py::commit_request`, вызывается сразу после `call_next`): порядок теперь «обработчик → намерения + коммит → ответ». Ответ 4xx/5xx не коммитится вовсе, сбой коммита превращается в 500 вместо ложного 2xx. `get_session` остался владельцем сессии и отката. Класс маршрута для этого не годится: `include_router` сохраняет `route_class` вложенного роутера, и переопределение на приложении не применяется.

[2026-09-20 12:00] [Интеграция -> все] F0 выполнен: `upstream/main` (бэк Ф4/Ф5, 26 коммитов, 147 файлов) влит в ветку `integration/f0-upstream-merge` без конфликтов; `frontend/` не тронут. `alembic upgrade head` без изменений — `0005_merge_heads` из upstream уже сводит `0003_user_name`/`0004_student_state` с `0003_phase4`/`0004_phase5`. `make types` даёт пустой дифф: X1 из ТЗ закрыт, шаг CI «Check frontend API types» должен быть зелёным. Юнит-тесты бэка: 1006 passed. В корневой `Makefile` добавлены `web`, `dev` и `types-check`.
[2026-09-20 12:00] [Интеграция -> B3] `scripts/gen_types.sh` на этой машине отрабатывает в git bash (правка про `-x` из `docs/frontend-backend-integration.md` §8 более не воспроизводится). `uv run --frozen` поднимает `create_app()` без живого стека.
[2026-09-20 12:00] [Интеграция -> F1, F2] X7 закрыт: `frontend/src/components/account/store.ts` переведён на `api/client.ts`. 401 на доменном вызове ведёт на `/login?next=`, `/auth/*` сохраняет свой 401 (неверный пароль и «не вошёл» — это ответы, а не протухшая сессия). Идемпотентный GET повторяется один раз через 300 мс при status 0 и 502/503/504. `PATCH /state` батчится по 200 ключей (лимит `app/api/state.py`).
[2026-09-20 12:00] [Интеграция -> F1, F2] X2 закрыт: `quack/remoteSource.ts` переписан под Ф4 (`GET /quack`, `POST /quack/seen|{id}/accept|{id}/decline`). Обновление — поллинг 60 с, возврат фокуса и перечитывание после действий; `EventSource`, `/quack/state` и `POST /events` убраны. Вся трансляция `QuackOut → QuackState` — в новом `quack/remoteAdapter.ts` под тестами (18 кейсов).
[2026-09-20 12:00] [Интеграция -> F1, F2] Отступление от ТЗ §3.2 «contract.ts не меняем»: в `Signal` добавлено необязательное поле `recommendation {id, actionText, status}`, в `QuackSource` — необязательные `accept`/`decline`. Иначе кнопку «принять» из Ф4 некуда положить: `Signal` умеет только вехи и варианты конфликта. Изменение аддитивное, локальный источник и экраны не трогались.
[2026-09-20 12:00] [Интеграция -> F1] Quack пока остаётся за явным `NEXT_PUBLIC_QUACK_SOURCE=remote` и НЕ следует за `NEXT_PUBLIC_DATA_SOURCE`: в `GET /quack` нет ни готовности, ни шансов по сохранённым программам — они из `/overview` и `/matching`. Снять ограничение вместе с F3.3.
[2026-09-20 12:00] [Интеграция -> все] Во фронт добавлен vitest (`npm test`) — без него приёмка §7.1 (юнит-тесты адаптеров, тест SSE-парсера) невыполнима. Покрыты `api/stream.ts` и `quack/remoteAdapter.ts`; шаг в CI ещё не заведён.

[2026-09-20 13:15] [Интеграция -> B1, БЛОКЕР] `POST /diagnostic` отдаёт `next_task`, но НЕ кладёт его id в `state.asked`. `app/apply/diagnostic.py::_issue_next` выдаёт задачу и возвращает её, не трогая состояние и не сохраняя run. Следствие: `POST /diagnostic/{run}/answer` всегда отвечает 404 `task instance not found in diagnostic run` (проверка `body.instance_id not in state.asked` в `app/api/diagnostic.py:128`), а если бы прошла — `apply_diagnostic.answer` упал бы на `ValidationFailed("no asked tasks")`. Входной замер непроходим. Воспроизводится на чистом аккаунте: register → `POST /diagnostic {exam_id, n_tasks:null}` → 201 с `next_task` и `asked: []` → answer 404.
[2026-09-20 13:15] [Интеграция -> B1, БЛОКЕР] `POST /diagnostic/{run}/finish` всегда 500. `app/apply/diagnostic.py:198` пишет событие `diagnostic.completed` с `payload={"run_id": ...}`, а frozen-схема `DiagnosticCompletedPayload` требует ещё `result: DiagnosticResult` → `ValidationError` в `events/store.py::_validated_payload`. В браузере это видно как `net::ERR_FAILED`: у 500 из необработанного исключения нет CORS-заголовков, поэтому фронт не может прочитать даже код ошибки.
[2026-09-20 13:15] [Интеграция -> B3] Предложение к §5.5: 500 должен отдаваться через тот же обработчик, что и остальные ошибки, чтобы CORS-заголовки были на месте. Иначе любая внутренняя ошибка на кросс-origin стенде неотличима от обрыва сети.
[2026-09-20 13:15] [Интеграция -> B3] `DiagnosticStartIn.n_tasks: int | None` без значения по умолчанию — поле обязательное, но nullable. Клиент обязан слать `"n_tasks": null`; пропуск даёт 400 `validation_failed`. Либо `= None`, либо строка в контракте.
[2026-09-20 13:15] [Интеграция -> F1, исправлено] `DiagnosticMock.tsx` при отказе бэка молча оценивал ответ локально и показывал выдуманный итог «0 / 8». Теперь при `submitDiagnosticAnswer`/`finishDiagnosticRun` == null показывается прямой текст об отказе, выбор сбрасывается, итог не рисуется (ТЗ §6, X10). Проверено в браузере на живом стенде.
[2026-09-20 13:15] [Интеграция -> F1, исправлено] Адаптер Quack называл «Не успеваешь» случай, когда `forecast.ready_by` и `on_track` оба null, — а бэк в этом же ответе пишет «готовность не посчитать — часы не указаны». Добавлен вердикт «Пока не посчитать»; уровень остаётся худшим (ничего не обещаем). Зафиксировано тестом на записанном живом ответе `/quack` (`live-quack.fixture.json`).
[2026-09-20 13:15] [Интеграция -> F1] `DiagnosticMock` шлёт два `POST /diagnostic` подряд (первый получает 409, второй 201) — гонка между проверкой `/diagnostic/active` и стартом при двойном монтировании. Обработчик 409 выправляет ситуацию, так что это косметика, но в приёмке §7.3 п.5 это тот же класс дефекта.
[2026-09-20 13:15] [Интеграция -> все] Прогон сценария жюри на живом стеке. Проходит: регистрация → `/state` → анкета в чате (агент пишет профиль, панель совпадает с `GET /profile`) → подборка из каталога бэка с текстами реалистичности → сохранение в избранное (`POST /saved` 201) → сравнение (локальное, `/matching/compare` не вызывается — X5 открыт). Обрывается на подготовке: входной замер непроходим (см. выше), из-за чего `GET /sets` отдаёт `current: null` и сет без тем, а фронт молча показывает локальный `prepData.ts`. Шаги «ответ на задачу → карта знаний → пересборка сета → рекомендация Quack» на бэке не проверены — они за блокером.
[2026-09-20 13:15] [Интеграция -> все] Сид на этой машине неполный: шаг `misconceptions` не проходит (модель эмбеддингов не грузится, OpenBLAS/paging file при 2 ГБ свободной памяти). Остальные шаги прогнаны отдельно: `--only templates,knowledge_base,calendars,programs` → templates 123 узла, knowledge_base 12, calendars 10, programs 29. Воркеры на Windows требуют `PYTHONIOENCODING=utf-8` (structlog падает на кириллице в cp1251) и `OPENBLAS_NUM_THREADS=1`.

[2026-09-20 14:00] [Интеграция -> B1] Оба блокера замера закрыты, плюс третий того же рода, который прятался за первым.
  1. `apply/diagnostic.py::_issue_next` теперь кладёт настоящий `task_instance.id` в `state.asked` и сохраняет состояние (принимает `run_id`). Из `knowledge/diagnostic.py::apply_answer` убрана заглушка `"asked": [*state.asked, _make_uuid()]` (и сам `_make_uuid`): чистый слой не знает id инстанса, а из-за заглушки `asked[-1]` указывал в несуществующую задачу. Список ведёт слой I/O.
  2. `finish` кладёт в `diagnostic.completed` ещё и `result` — `DiagnosticCompletedPayload` его требует.
  3. Найдено по ходу: `answer` слал `diagnostic.progress` без `state`, который требует `DiagnosticProgressPayload`. Это был бы ровно такой же 500 сразу после снятия 404 — то есть замер всё равно остался бы непроходимым.
  Проверено сквозняком на живом стеке: register → `/auth/me` → `POST /diagnostic` → 9 ответов подряд (200, бюджет 8→0, спуск по предпосылкам работает) → `finish` 200 с результатом → `GET /diagnostic/active` 404. Юнит-тесты 1009 passed, ruff чист.
[2026-09-20 14:00] [Интеграция -> B1] Добавлены три регресс-теста в `tests/apply/test_diagnostic.py`. Дефекты дожили до сюда потому, что `tests/api/test_diagnostic_mocks_phase2.py` подставляет `asked=[instance_id]` руками, а payload событий не валидировался ни одним тестом. Новые тесты проверяют, что выданная задача попадает в `asked` и сохраняется, и что payload `diagnostic.progress` и `diagnostic.completed` проходят настоящий `events.store._validated_payload`. Каждый проверен на красноту: с возвращённым дефектом падает именно он.
[2026-09-20 14:00] [Интеграция -> B3] Узел `Student` в Neo4j создаётся только в `GET /auth/me` (`app/api/auth.py:159`). Любой пишущий в граф путь, выполненный до первого `/auth/me`, падает на `assert rec is not None` в `graph/queries/personal.py::merge_evidence` — то есть 500 без объяснения. Браузер это скрывает (он всегда зовёт `/auth/me` на загрузке), скрипты и интеграции — нет. Похоже на место для `ensure_student` в регистрации или в самом `merge_evidence`.

[2026-09-20 14:10] [Интеграция -> F1, исправлено] Числовые задачи замера были непроходимы в UI. Бэк выдаёт в замере и `type: "numeric"` (`options: []`), а `DiagnosticMock` рисует только варианты — на втором же вопросе экран оказывался без единого способа ответить. Раньше этого никто не видел: до фикса бэка до второго вопроса не доходили. Добавлены поле ввода и «Ответить»; признак ответа — `NUMERIC_ANSWER = -1`, он не совпадает ни с одним индексом варианта.
[2026-09-20 14:10] [Интеграция -> все] Замер пройден в браузере на живом стеке после перезапуска API: 8 вопросов (смешанные — варианты и числовые), итог с бэка — «твёрдо: 3 навыков, шатко: 4 навыков, подозрений на ловушки: 3», списки firm/shaky/ловушек отрисованы. Модель знаний обновилась: у отвечавшихся навыков `n_evidence: 1`, `p_recall` сдвинулся, записаны 2 misconception. `POST /diagnostic/{id}/finish` 200, run закрыт.
[2026-09-20 14:10] [Интеграция -> F1] Счёт на экране итога («1 / 7») считается локально по `answers` в React-стейте и расходится с сервером (тот записал 8 ответов). Надо брать из `DiagnosticResult`/`state.answered`, а не из стейта компонента — иначе после любой перезагрузки страницы посреди замера цифра врёт.
[2026-09-20 14:10] [Интеграция -> B1] После `finish` активный сет не открывается: `GET /sets?exam_id=ENT_MATH` отдаёт `current: null` и один `upcoming`. Фронт при этом показывает локальный сет SAT Math под заголовком «ЕНТ · МАТЕМАТИКА». Нужно решить, кто открывает первый сет после замера — правило на бэке или явный `POST /sets/{id}/open` с фронта.
[2026-09-20 14:10] [Интеграция -> B1] В `DiagnosticResult` один и тот же навык может попасть и в `firm`, и в `shaky` (`ent.func.linear` в живом прогоне), а `suspected` содержит дубликаты (`lib.inequality_endpoint` трижды). Экран показывает это как есть. Похоже на `diag_logic.finish`: списки накапливаются без дедупликации и без учёта повторного вопроса.

[2026-09-20 14:35] [Интеграция -> F1, F2] F3.3 сделан. Дашборд при `remote` собирается из `GET /quack` + `GET /overview` + `GET /matching` + `GET /saved`. Вся склейка — в новом `quack/remoteStanding.ts` чистыми функциями под тестами (13 кейсов): готовность из `progress` (берём худший экзамен — «готов к одному и не готов ко второму» это не «наполовину готов»), шансы из `MatchOut.realism` и жёстких факторов, вехи и конфликты из `/overview` в `alerts` и `next`. Экраны не менялись, `contract.ts` расширен аддитивно (`ActivityDay`, `QuackState.activity`, `ChanceFact.key = "factor"`).
[2026-09-20 14:35] [Интеграция -> F1] Шансы фильтруются по `/saved`: `/matching` отвечает про весь каталог, а карточка — про план ученика («всё, что следует из твоих сохранённых программ»). Без фильтра в «Программах» оказывался весь каталог из 29 позиций.
[2026-09-20 14:35] [Интеграция -> F1] Поллинг раз в 60 с перечитывает только `/quack`. `/overview`, `/matching` и `/saved` меняются лишь от действий ученика, а каждое такое действие и так зовёт `report()` — иначе минутный таймер стоил бы четырёх запросов вместо одного.
[2026-09-20 14:35] [Интеграция -> F1] Снято ограничение из записи 13:15: `NEXT_PUBLIC_QUACK_SOURCE` снова наследует `NEXT_PUBLIC_DATA_SOURCE`, потому что готовность и шансы теперь есть. Проверено в браузере на живом стенде: темп с вердиктом «Пока не посчитать» и словами бэка, четыре варианта темпа с действиями, в «Программах» одна сохранённая программа с уровнем «реалистично», активность с сервера.
[2026-09-20 14:35] [Интеграция -> все] Контейнеры Postgres/Neo4j/Redis упали (exit 255) при нехватке памяти, когда одновременно шли vitest, pytest, Next dev и API; после `docker compose up -d` пул Postgres в уже запущенном API не восстанавливается сам — API надо перезапускать. На машине с 16 ГБ и ~2 ГБ свободных это воспроизводится легко.

[2026-09-20 14:45] [Интеграция -> F1] F2 сделан. «Начать заново» теперь чистит и анкету на сервере: эндпоинта, стирающего профиль, нет, поэтому каждый заполненный слот гасится тем же `PATCH /profile` со значением `null`. Шлём только реально заполненные слоты — пустая анкета не стоит ни одного запроса, и агент не получает пачку `profile.updated` о полях, которых никто не заполнял. Проверено на стенде: после «начать заново» `GET /profile` не содержит ни одного значения, `readiness: 0`, `GET /saved` пуст.
[2026-09-20 14:45] [Интеграция -> B3] Просьба: `DELETE /profile` (или `POST /profile/reset`) сделал бы это одним запросом и одним событием вместо N. Сейчас обходимся фронтом, контракт не трогали.
[2026-09-20 14:45] [Интеграция -> F1] Шкала реалистичности сведена в одну таблицу `catalog.REALISM_LEVEL`, типизированную по `MatchOut["realism"]`; её же использует адаптер дашборда (раньше в `remoteStanding.ts` лежала вторая копия с `Record<string, Level>`, то есть без проверки полноты). Покрыта тестом: новый вердикт на бэке уронит сборку, а не карточку.
[2026-09-20 14:45] [Интеграция -> B2, закрыт] Дефект `sat_score=1300` не воспроизводится: в Ф4 `agents/selection.py` нормализует значения >800 (делит и округляет), а описание инструмента прямо просит писать балл секции математики. Со стороны фронта убрана ответная догадка «если >800, считаем это суммой»: `remoteChat.ts` теперь всегда удваивает (панель мыслит в 1600, бэк — в секции до 800), потому что оба писателя держат шкалу бэка — агент нормализует, панель делит перед `PATCH`. Значение больше 800 в базе — это устаревшие данные, а не сумма.
[2026-09-20 14:45] [Интеграция -> B3] `academics.sat_score` — `ProfileField[int]` без границ, так что `PATCH /profile` примет и 1300 (нормализация есть только в агенте). Ограничение верхней границей по шкале экзамена закрыло бы дыру в корне, но `app/schemas/profile.py` — frozen-контракт, поэтому только запись.

[2026-09-20 15:00] [Интеграция -> F1, F2] F5 сделан. Сравнение читает `GET /matching/compare?ids=`: таблица (арифметика) показывается сразу, вывод — когда готов; `collapsed_same` сворачивается в строку «одинаково по N параметрам». Локальный `compareRows` остаётся фолбэком и работает, пока идёт первый запрос. Карточка программы получила `realism_text` со статусами, «Уточняем соответствие…» при `soft_pending`, плашки «демо» (только при `is_demo`, раньше стояла у всех) и «извлечено автоматически», блок «Источник» со ссылкой и датой проверки.
[2026-09-20 15:00] [Интеграция -> F1] Жалоба «Данные неверны» показывается только у `extracted_auto`: выверенный вручную «пол» бэк помечать не даёт (409), и кнопка там просто не нужна. 409 всё равно обработан — экран говорит «эта запись выверена вручную», а не «не получилось».
[2026-09-20 15:00] [Интеграция -> F1] Поиск программ подключён там, где он и нужен: когда каталог отвечает пустой подборкой, ассистент запускает `POST /programs/search` по направлению и стране из анкеты, опрашивает статус с бэкоффом 2→30 с и не дольше 90 с, после `done` перечитывает каталог. `unavailable` и таймаут — разные фразы, обе честные.
[2026-09-20 15:00] [Интеграция -> B1, B3] Наблюдение со стенда: девять ответов замера подряд через API оставляют `sets.availability.reason = "projection_pending"`. Это фиксация из Ф5 D10 (без студенческого лока проекция откладывается), и быстрый скрипт упирается в неё там, где живой ученик не упёрся бы. Свип разбирает backlog штатно (`recovery_sweep queued=1 students=1`, `graph_pending` уходит из `/health`), просто крон ходит раз в 10 минут — мои более ранние проверки попадали между запусками.
[2026-09-20 15:00] [Интеграция -> B1, ОТКРЫТО] После разбора backlog `GET /sets?exam_id=ENT_MATH` всё равно отдаёт `current: null` и один `upcoming` с пустым `topics` (`kind: consolidation`, «закрепление перед тестом»). При этом замер нашёл пять шатких навыков, и `GET /knowledge` показывает у них `n_evidence: 1` и сдвинутый `p_recall`. Похоже, очередь (`app/sets/queue.py`) для ЕНТ выходит пустой, и `assemble_sets` собирает только финальный consolidation. Ещё примета: `upcoming[0].id` меняется при каждом чтении — план пересобирается на лету и не сохраняется.
[2026-09-20 15:00] [Интеграция -> F1] Из-за этого живьём не проверены F4 (теория темы и отчёт по сету): для `GET /texts/{set}/{skill}` нужен сет с темами. Код собран, типы и тесты зелёные, контракты сверены по `app/api/texts.py`, `app/api/programs.py` и `app/api/matching.py`.

[2026-09-20 15:05] [Интеграция -> F1, F2] F6 сделан. В `store.ts` появился явный белый список ключей с обоснованием на каждый (ТЗ §5.6): ключ не из списка не уходит на сервер вовсе, а в консоль падает предупреждение с именем файла. Так доменный срез не вернётся в `/state` молча — для этого придётся дописать строку в список.
[2026-09-20 15:05] [Интеграция -> F1] `quack-choice-workspace` при `remote` больше не хранит анкету, избранное и переписку: всё это читается с сервера при каждой загрузке и всё равно перезаписывает сохранённое. Осталось экранное — какие программы показаны и какие в сравнении. Проверено на стенде: `GET /state` содержит только `quack-choice-layout` и `quack-choice-workspace`, анкета в нём пуста.
[2026-09-20 15:05] [Интеграция -> F1, ОСТАЛОСЬ] `quack-prep` пока хранится целиком. Внутри и домен (states, recall, evidence, misconceptions, doneSets, currentSet), и то, что по §5.7 честно остаётся на фронте (материалы — конспекты и карточки). Разделять его вслепую не стал: подготовка сейчас не проверяется живьём (сеты приходят без тем), а это ровно тот код, где регрессию не увидишь без прогона. Ключ оставлен в списке с этой пометкой.

[2026-09-20 15:20] [Интеграция -> B3, исправлено] §7.3 п.1 («останови Neo4j — остальное живо») не проходил: при упавшем Neo4j `GET /auth/me` отвечал 500, и войти было нельзя вовсе. Guard в `app/api/auth.py` покрывал только `graph is None` (граф не поднялся на старте), а не падение после старта — тогда MERGE в `ensure_student` бросает. Добавлена обработка `ServiceUnavailable`/`SessionExpired`: личность не зависит от графа (product-logic §6.3), узел создастся следующим `/auth/me`. Регресс-тест `test_me_answers_when_neo4j_goes_down`, проверен на красноту.
[2026-09-20 15:20] [Интеграция -> B1, B3, ОТКРЫТО] Там же и по той же причине: при упавшем Neo4j `GET /matching` и `GET /knowledge` отвечают 500 вместо мягкого отказа, который обещают §6 и `AvailabilityOut`. `/profile` выживает. Не чинил — это внутренности зон B1 (`matching`, `knowledge`) и трогать их вслепую рискованно, но для сценария жюри это значит «Neo4j лёг — подбор и карта легли вместе с ним».
[2026-09-20 15:20] [Интеграция -> B3] Отдельно: 500 из необработанного исключения уходит без CORS-заголовков, поэтому в браузере любая такая ошибка выглядит как обрыв сети, а не как ответ сервера. Пока это так, фронт не может отличить «сервер упал» от «нет связи» и честно сказать об этом ученику.
[2026-09-20 15:20] [Интеграция -> F1] §7.3 п.4 проходит: после протухания cookie очередной доменный запрос (`/quack`, `/overview`, `/matching`, `/saved` — все 401) уводит на `/login?next=%2Fchoice`, проверено в браузере.

[2026-09-20 15:45] [Интеграция -> B1, закрыто] Запись 15:00 разобрана. Архитектура на месте: сеты живут в Postgres как read-модель (`sets`/`set_topics`, ссылки на `skill_id` в граф), `_read_sets` — чистый SELECT, пишут план только правила по событиям (анкета, замер, мок, ответ на задачу, наблюдатель, сохранённая программа, выбор сета, сдвиг срока). Пустой план шёл не оттуда.
[2026-09-20 15:45] [Интеграция -> B1] Первая причина (уже была починена в `f40cb67`): `assemble_sets` уводил все навыки с `confidence < c_vis` в `checks`, а не в темы. У нового ученика таких — все, и план схлопывался в один consolidation без тем. Сейчас для ENT_MATH собирается 9 обычных сетов по 3 темы + закрепление (25 навыков в графе, p_target 0.95, очередь непустая).
[2026-09-20 15:45] [Интеграция -> B1, B3] Вторая причина — меняющиеся `id` — была в `GET /sets`: маршрут звал `rebuild_sets`, когда в плане нет ни одного не-consolidation сета. При старом ассемблере это условие было вечно истинным — план пересобирался на каждом чтении, а `replace_plan` удаляет все `upcoming` и создаёт их заново с новыми uuid. Условие убрано.
[2026-09-20 15:45] [Интеграция -> B1, B3] Почему это важно и за пределами пустого плана: по `set_id` ключаются ссылки экранов, кэш сгенерированных текстов (`GET /texts/{set}/{skill}`), отчёт по сету и снимок прогноза `opened_forecast`. Пересборка на чтении выдёргивала их из-под ученика между двумя запросами одного экрана.
[2026-09-20 15:45] [Интеграция -> B1, B3] Что оставлено в `GET /sets`: единственная запись — холодный старт, когда для экзамена в Postgres нет ни одной строки. Она заведомо одноразовая: после сборки строки есть, ветка больше не берётся. Альтернатива (совсем чистый SELECT) означала бы пустую «Подготовку» у ученика, который ещё не дал ни одного события; если B3 захочет убрать запись из GET совсем, нужен явный триггер (событие регистрации или `POST /sets/plan`).
[2026-09-20 15:45] [Интеграция -> все] Проверено на живом стенде: три чтения `GET /sets?exam_id=ENT_MATH` подряд возвращают одни и те же десять `id`; свежезарегистрированный ученик получает план на первом чтении и те же `id` на втором. Регресс-тест `test_a_plan_of_one_consolidation_set_is_not_rebuilt`. Бэкенд: 1011 тестов, ruff чист.
[2026-09-20 15:45] [Интеграция -> B1] На будущее: `_with_names` на каждом `GET /sets` дёргает `list_exam_skills` в Neo4j ради названий навыков — в `set_topics` колонки `name` нет. Не трогал (чтение с мягким отказом и снимком-фолбэком), но если граф станет узким местом — имя темы дешево кладётся в read-модель рядом с `skill_id`.
