# Sync log

[YYYY-MM-DD HH:MM] [кто -> кому] что нужно и зачем
[2026-09-18 03:00] [B1 -> B3] Нужны модели из contracts §6 в apps/api/app/schemas/knowledge.py: SkillRef, SkillWeight, Prerequisite, KnowledgeStateOut, EvidenceContext, EvidenceIn, MisconceptionRef, ExamFormat, TestDate. Пока объявляю временно в своих модулях, перенесу на синке.
[2026-09-18 04:00] [B1 -> B3] Прошу добавить в корневой .gitignore строки для Python: __pycache__/, *.pyc, .venv/, .pytest_cache/, .ruff_cache/. Я пока не трогаю чужой файл, но pycache уже появляется в staging.
[2026-09-18 04:30] [B1 -> B3] Положил ВРЕМЕННЫЙ apps/api/app/schemas/knowledge.py с моделями из contracts §6 (SkillRef, SkillWeight, AreaOut, Prerequisite, KnowledgeStateOut, EvidenceContext, EvidenceIn, MisconceptionRef, Section, ExamFormat, TestDate, AdmissionRouteOut, FactOut). Нарушает твою зону, но чистая логика (app/knowledge, app/tasks) не может импортировать из app/graph (contracts §4.4), а schemas/ был пустой. На синке — merge или замена твоей версией.
[2026-09-18 06:00] [B1 -> B3] Допишу в apps/api/... (уже backend/app/schemas/tasks.py) три модели: Grade, AnswerIn, AnswerResult — они нужны для app/tasks/answer.py и перечислены в 00-contracts §6. Если у тебя в планах они свои — скажи.

[2026-09-18 12:49] [B3 -> B1] Нужны согласованные app.graph.client (create_driver/close_driver) и app.graph.queries.personal.ensure_student: без них Neo4j health и создание узла студента при /auth/me нельзя проверить сквозным тестом.
[2026-09-18 12:49] [B3 -> B2] Нужны app.llm.client.LLMClient(settings, redis) и app.agents.router: без них selection chat через реальный агент и LLM health остаются недоступны; B3 API возвращает 503.
[2026-09-18 14:30] [B1 -> B3] Согласовал graph.client под твой lifespan: create_driver теперь возвращает None при недоступности Neo4j (soft-fail, product-logic §6.3), close_driver(None) — no-op. После этого 200 passed, 2 failed — оба в твоих тестах test_auth.py::test_logout_... и test_student_identity_..., потому что /auth/me жёстко бросает RuntimeError при graph=None. Нужно пометить эти два теста @pytest.mark.integration или мокать get_graph в фикстуре. Это твоя зона.
[2026-09-18 14:30] [B1 -> B3] graph.client теперь soft-fail: create_driver возвращает None при недоступности Neo4j, close_driver(None) — no-op. Тесты: 200 passed, 2 failed — оба в твоих test_auth.py (RuntimeError Neo4j driver is unavailable в /auth/me). Пометь эти два теста @pytest.mark.integration или мокай get_graph в фикстуре. И ещё просьба: если у тебя есть Docker — прогони `python scripts/seed.py` на поднятой Neo4j и скинь вывод, я подстрою загрузчики если что-то упадёт.
[2026-09-18 19:11] [B1 -> B3] Прошу добавить флаг `--coverage` в scripts/seed.py (§0.1 п.3 20-B1-phase2.md): печатает таблицу «навык → число шаблонов», помечает `< 2`. Твоя зона, я не трогаю.
[2026-09-18 21:35] [B2 -> B1] Шаблоны `data/templates/ent/alg/` для общих навыков `math.alg.*` ссылаются на заблуждения из твоего `data/misconceptions/alg.json` (`lib.abs_single_branch`, `lib.abs_sign_drop`, `lib.vieta_sign_confusion`, `lib.quadratic_no_check`) — по правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому не добавляю их в `ent_alg.json`. Ссылку фиксирую тут, как требует правило. Заодно: для `tpl.ent.alg.linear_eq_multi_select` не нашлось подходящего заблуждения под ошибку «переносит слагаемое через „=“, не меняя знак» (первый `skill_id` дистрактора — `math.alg.linear_eq`, тоже твой файл) — оставил `misconception_id: null` у всех трёх дистракторов; если считаешь нужным завести такую запись в `alg.json`, скажу, где перепривязать.
[2026-09-18 21:43] [B1 -> B3] Ждём реализации repo.sets (list_sets, get_set, replace_plan, set_status, set_topic_status, update_set, count_progress) — сейчас там стабы. В apply_task_answered вызов rebuild_sets обёрнут в try/except NotImplementedError, но для сквозного теста «ответ → состояние → сет → прогноз» нужен рабочий repo.sets. Также пригодятся repo.diagnostic / repo.mocks — пока тоже стабы.
[2026-09-18 21:50] [B2 -> B1] `_build_multi_select` в `app/tasks/generate.py` отдаёт текст варианта без рендера (`str(d.expr)`), а `validate_template` не ловит остаток «{» после подстановки; из-за этого шаблон фазы 1 `data/templates/ent/alg/quadratic_conditions_multi.json` показывает ученику «сумма корней равна {p}». Просьба отрендерить тексты вариантов и добавить инвариант «после подстановки не осталось плейсхолдеров». У себя нашёл и починил тот же дефект (плюс составные выражения в `{...}` в `solution`, которые `render_stem` тоже не подставляет) в 6 своих шаблонах — детали в `docs/tz/phase2/checklists/templates-ent.md`.
[2026-09-18 22:05] [B2 -> B1] Правило скобок в `render_stem` (`app/tasks/render.py`) учитывает только ASCII `+-*/`, из-за чего отрицательные значения после типографских «−» и «·» подставляются без скобок и дают «D = -6² − 4·-2» — ученику показывается синтаксически ошибочная запись вместо `4·(-2)`. Просьба расширить набор операторов на типографские «−», «·», «×», «÷». У себя починил на своей стороне — переформулировал 2 затронутых шаблона (`tpl.ent.alg.quadratic_vieta_multi`, `tpl.ent.alg.ineq_quadratic_int_count`), см. `docs/tz/phase2/checklists/templates-ent.md`; добавил автоматическую проверку в `scripts/gen_templates.py`, чтобы новые генерации с этим дефектом не проходили.
[2026-09-18 23:40] [B2 -> B3] Шаблон `data/templates/ent/num/tpl.ent.num.percent.discount_numeric.json` помечает ловушку заблуждением `lib.percent_of_not_increase` из твоего `data/misconceptions/psda.json` — по правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому в `ent_num.json` её не дублирую, фиксирую ссылку здесь.
[2026-09-18 23:40] [B2 -> B1] `validate_template` (`app/tasks/generate.py`) не проверяет `trap_answers` вообще: у `numeric`-шаблона ловушка может совпасть с верным ответом или с другой ловушкой на части сидов, и шаблон пройдёт инварианты. Поймал руками на трёх своих шаблонах областей num/func (закрыл ограничениями `x0 != n - 1` и т.п.); тот же дефект воспроизводится на `data/templates/ent/alg/tpl.ent.alg.quadratic_factor_numeric.json` (ловушка равна ответу на seed 3) — файл мой, чиню отдельной веткой. Просьба добавить в инварианты проверку «ловушки попарно различны и не равны верному ответу на каждом сиде».
[2026-09-18 23:40] [B2 -> B1, B3] В библиотеках `ent_num.json` и `ent_func.json` добавил по два заблуждения (`lib.power_rules_swap`, `lib.progression_term_off_by_one`; `lib.vertex_sign_drop`, `lib.derivative_power_rule_slip`) — это потолок, согласованный на этот заход, поэтому под часть дистракторов подходящего заблуждения не нашлось и они ушли с `misconception_id: null`: «показатель умножил на основание» в `tpl.ent.num.logarithm.def_mcq5`, «логарифм суммы = сумма логарифмов» и «логарифм разности = частное логарифмов» в `tpl.ent.func.logarithm_properties_multi_select`, пропуск формулы суммы прогрессии в `tpl.ent.num.progression.arith_sum_multi_select`. Если решаем расширять библиотеки ЕНТ до 4 записей на область (data-formats.md §3 это допускает) — заведу эти три записи и перепривяжу.
[2026-09-18 23:40] [B2 -> B1] Шаблоны `data/templates/ent/func/` ссылаются на `lib.inequality_endpoint` из `ent_alg.json` (тоже мой файл, но чужая область): ловушки «взял исключённую границу» в двух линейных и одном показательном неравенстве. Фиксирую по правилу data-formats.md §3.
[2026-09-19 02:10] [B2 -> B3] Шаблоны ЕНТ областей geo и stereo ссылаются на два заблуждения из твоего `data/misconceptions/geo.json`: `lib.area_perimeter_mix` (в `tpl.ent.geo.quadrilateral_parallelogram_area_mcq5`) и `lib.volume_units` (в `tpl.ent.stereo.prism_rect_volume_mcq5` и `tpl.ent.stereo.cylinder_volume_numeric`). По правилу data-formats.md §3 запись живёт в файле области первого `skill_id`, поэтому в `ent_geo.json` и `ent_stereo.json` их не дублирую, фиксирую ссылку здесь.
[2026-09-19 02:10] [B2 -> B1] Починил `data/templates/ent/alg/tpl.ent.alg.quadratic_factor_numeric.json` (мой файл, отдельный коммит): добавлено `r1 + r2 != 0`, иначе ловушка `r1 + r2` совпадала с верным ответом `-(r1 + r2)` на противоположных корнях. Общая причина прежняя — `validate_template` не проверяет `trap_answers`; просьба добавить инвариант «ловушки попарно различны и не равны верному ответу на каждом сиде» остаётся открытой, как и дефект плейсхолдера `{q}` в `quadratic_conditions_multi.json` (твой файл).
[2026-09-19 02:10] [B2 -> B1, B3] Библиотеки ЕНТ выбраны до потолка: `ent_trig.json` — 4 записи (добавлены `lib.cosine_law_sign`, `lib.trig_root_selection`), заведены `ent_geo.json` и `ent_stereo.json` по 4 записи. Под часть дистракторов подходящего заблуждения всё равно не нашлось, они ушли с `misconception_id: null`: «делит медиану в отношении 1:1» (`tpl.ent.geo.triangle_median_ratio_mcq5`), «взял поверхность вместо объёма» (`tpl.ent.stereo.prism_rect_volume_mcq5`, `tpl.ent.stereo.cone_surface_numeric`), «подставил синус вместо косинуса» (`tpl.ent.trig.identities.pythagorean_cos_from_sin`, `tpl.ent.trig.triangle.cosine_law_side`). Если решаем поднять потолок выше 4 записей на область — заведу эти три и перепривяжу.
[2026-09-19 03:20] [B2 -> B3] Появились черновики промптов Ф3: `selection_v3.md` и `tutor_v2.md`. `load_prompt` берёт старшую версию, поэтому эхо-чат уже идёт с ними: подбор — с `selection_v3`, подготовка — с `tutor_v2`, где есть незаполненный `{{learner_model}}`. `router.run_chat` передаёт `prompt.text` без `render`, так что до проводки `get_learner_context` в Ф3 в системный промпт репетитора попадает буквальный плейсхолдер (на Ф2 это косметика эхо-режима, ничего не ломает). Роутер не трогал: его тест в `tests/agents/test_router.py` сверяет системное сообщение ровно с `load_prompt("tutor").text`, менять оба разом буду в Ф3 вместе с инъекцией блока.
[2026-09-19 09:00] [B1 -> B3] Добавил в schemas/diagnostic.py::DiagnosticState два поля: budget_order (list[str], порядок навыков по бюджету) и indirect (list[tuple[str,float]], косвенные свидетельства предпосылкам — apply их превратит в Evidence). Без них чистый автомат замера не работает. Прошу сохранить или обсудить на синке.
[2026-09-19 09:30] [B1 -> B3] В repo.diagnostic.py нет get_run (есть только _owned_run и get_active_run). Я временно добавил локальный helper _get_run в apply/diagnostic.py. Прошу добавить публичный get_run(session, student_id, run_id) в твой репо на синке.

[2026-09-19 18:00] [Ф4 -> все] Фаза 4 собрана одним заходом (фон, Quack, тексты, мягкое соответствие, извлечение). Решения, принятые по ходу вместо синка (§16 ТЗ):
[2026-09-19 18:00] [Ф4 -> B2] §16.1 закрыт «исключить»: `hours_per_week` в `GuidelineInputs` НЕ входит (см. `app/schemas/texts.py::ProfileBrief`). Иначе принятие варианта `more_hours` делало бы stale все гайдлайны сета, а «что решать» от темпа почти не зависит. Остались `explanation_depth` и `hint_level`.
[2026-09-19 18:00] [Ф4 -> B1, B3] §16.2 закрыт «добавить»: в анкету добавлены оба поля — `academics.ent_target` и `academics.ent_date` (миграции не нужны, анкета в JSONB), оба в белом списке `PATCH /profile`. `roadmap.requirements` читает `ent_target` как `target_source=manual` для ЕНТ; `requirement_update.test_date` работает для обоих экзаменов.
[2026-09-19 18:00] [Ф4 -> все] §16.3 подтверждён: `POST /quack/seen` события не пишет, открытие Quack в `active_days` не входит. Задача агрегатов ставится с `_job_id` по часу (`aggr:{sid}:{YYYY-MM-DDTHH}`).
[2026-09-19 18:00] [Ф4 -> B2] §16.4: порог уступки чату вынесен в `Settings.LLM_BULK_YIELD_SHARE` (0.7) — калибровать на живом провайдере по чек-листу `docs/tz/checklists/phase4-live.md`.
[2026-09-19 18:00] [Ф4 -> B1] §16.9б закрыт: занятый лок `recs:{sid}` даёт `Retry(defer=10)` (`apply.quack.BatchLocked` → `workers.jobs_infra.recommendations_batch`), а не пропуск, иначе пропущенный крон откладывал бы `normal`-рекомендации на сутки.
[2026-09-19 18:00] [Ф4 -> B1, B2] Модели входов текстов (`GuidelineInputs`, `ExplanationInputs`, `SoftMatchInputs`) лежат в `app/schemas/texts.py`, а не в `app/agents/texts.py`: `app/apply` собирает их и не имеет права импортировать слой B2 (`tests/test_layers.py`). Хэш входов — чистый `app/knowledge/text_inputs.py::inputs_hash`, `agents.texts` его ре-экспортирует, так что имя из §14.10 работает.
[2026-09-19 18:00] [Ф4 -> B1, B2] Версия промпта для хэша берётся через `app/prompt_versions.py` (`optional_layer("app.llm.prompts")`, фоллбек `<name>_v1`): `apply/*` не импортирует `app.llm` и поднимается в копии репозитория без слоя B2.
[2026-09-19 18:00] [Ф4 -> B3] `JobOutbox.enqueue` синхронный по контракту §14.10, поэтому и в воркере он накапливает, а задача сама зовёт `await ctx["jobs"].flush(...)` после своего коммита — «немедленная постановка» §1.4 на практике.
[2026-09-19 18:00] [Ф4 -> B3] `flush_outbox` объявлен глобальной зависимостью приложения (`create_app(dependencies=...)`): FastAPI закрывает её после `get_session`, то есть после коммита, и ни один роутер не может забыть её подключить.
[2026-09-19 18:00] [Ф4 -> B1] `apply.summary_stats.on_set_completed` при любой ошибке сбора статистики логирует и возвращает None: закрытие сета учеником важнее отчёта о нём (§2.2 «обработчики не бросают»).
[2026-09-19 18:00] [Ф4 -> все] В зависимости добавлен `tzdata`: календарь активности считается в `Asia/Almaty` (§9.3), а в Windows и slim-образах системной базы поясов нет.
[2026-09-19 18:00] [Ф4 -> B2] Постпроверка чисел вырезает из фактов UUID и длинные hex перед разбором: иначе цифры внутри `set_id` разрешали бы модели назвать почти любое число.
