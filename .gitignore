# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets
.env
.env.*
!.env.example

# local TZ notes and CI logs, not the pytest suite
backend/tests/*.md
backend/tests/error.txt
# local graphify output at the repo root (per-folder graphs are tracked)
/graphify-out/
